# 3.3.3 终局分派段

## 3. 终局分派段

### 源码细节(判定链, 顺序即优先级)

```
:1012  abort 信号最优先(用户意志最高)
        ├ 有 executor: getRemainingResults() 排干
        │   executor 检查 abort 信号 → synthetic tool_result
        ├ 无 executor: yieldMissingToolResultBlocks
        │   + 'Interrupted by user'
        ├ reason !== 'interrupt'(submit 型)→ 不注打断消息
        │   (后随 user message 已提供上下文)
        └ return aborted_streaming

:1053  pendingToolUseSummary 收割
        (上轮 haiku 摘要在模型流式期间已跑完: ~1s vs 5-30s)

:1063  needsFollowUp = false → 恢复链:
        ├ :1086 collapse drain(便宜先行)
        │    guard: transition ≠ collapse_drain_retry(防重复 drain)
        │    committed > 0 → continue
        ├ :1119 reactive compact
        │    成功 → hasAttemptedReactiveCompact = true
        │          → continue(reactive_compact_retry)
        │    media 错误只走此路 strip-retry(collapse 不剥图)
        └ 都失败 → surface + executeStopFailureHooks
             + return(prompt_too_long / image_error)
             ★ 不进 stop hooks(死循环防护, 见下)

:1193  isWithheldMaxOutputTokens:
        ├ 阶梯 1: :1210 升 64k 同请求重发(零 meta)
        └ 阶梯 2: :1230 meta 续写(限次, count+1)

:1259  isApiErrorMessage(任意 API 错误)
        └ 一律跳过 stop hooks → executeStopFailureHooks
           → return(rate limit / PTL / auth 同待遇)

:1267  stop hooks:
        ├ preventContinuation → return stop_hook_prevented
        └ blockingErrors → 注入 errors → continue(stop_hook_blocking)
           maxOutputTokensRecoveryCount 归零
           hasAttemptedReactiveCompact ★保留(死循环防护)

:1318  TOKEN_BUDGET:
        └ 预算未满 → nudge meta 注入
           → continue(token_budget_continuation)
           (此路径 hasAttemptedReactiveCompact 重置 false — 与上相反)

:1430  needsFollowUp = true → 工具等待:
        ├ 工具侧 abort → 补账 + abort 时也查 maxTurns
        │   → return aborted_tools
        └ shouldPreventContinuation → return hook_stopped
           → 都没有 → 进 4. 轮末收集
```

- 判定顺序的原则: 越靠前的越代表外部世界的硬约束(用户意志 > 上轮遗留 > 恢复 > hook > 预算)

### 总表

| # | 出口 | 触发条件 | 怎么做 | State 关键动作 |
|---|---|---|---|---|
| 1 | aborted_streaming | abort 信号 | 补账 → return | 不走 4., 预取废弃 |
| 2 | follow-up 主路径 | 有 tool_use | 等工具 → 进 4. | count 归零 |
| 3 | collapse_drain_retry | 413 | drain staged collapses(便宜) | transition 记因防重 |
| 4 | reactive_compact_retry | 413 / media | 全量压缩重试 | hasAttempted=true |
| 5 | max_output_tokens_escalate | 截断 + 未设 override | 升 64k 同请求重发, 零 meta | override=64k |
| 6 | max_output_tokens_recovery | 截断 + 64k 也不够 | meta 续写(限次) | count+1 |
| 7 | API error return | isApiErrorMessage | 跳过 stop hooks → return | — |
| 8 | stop_hook_blocking | hook 判不完整 | 注入 errors 重进循环 | hasAttempted 保留 |
| 9 | token_budget_continuation | 预算未满 | nudge meta 注入 | hasAttempted 重置 |
| 10 | 自然结束 | 无 tool_use 且 hook 放行 | return | — |

### 术语: follow-up(跟进)

follow-up = 跟进: 模型这轮用了工具, 工具结果必须再发给模型"回话", 循环因此进入下一轮。

源码开关(流式消费时):

```
const msgToolUseBlocks = message.message.content.filter(
  content => content.type === 'tool_use',
)
if (msgToolUseBlocks.length > 0) {
  needsFollowUp = true        ← 本轮出现了 tool_use
}
```

这个标志决定终局分派走哪条路:

```
needsFollowUp = false(本轮没有 tool_use)
  → 模型认为说完了
  → 走 stop hooks → return 自然结束
  → 一问一答, 不需要跟进

needsFollowUp = true(本轮有 tool_use)
  → 模型的回答依赖工具结果, 但结果还没给它
  → 等工具跑完 → 结果追加进 messages
  → 4. 轮末收集 → state = next → continue
  → 这就是 follow-up: 拿着结果回去"跟进"模型
```

一个完整例子:

```
用户: 修复这个 bug

轮 1  模型: 我先看看文件    [tool_use: Read]   → needsFollowUp → 跟进
轮 2  模型: 找到了, 来改     [tool_use: Edit]   → needsFollowUp → 跟进
轮 3  模型: 改完跑下测试     [tool_use: Bash]   → needsFollowUp → 跟进
轮 4  模型: 测试通过, 修复完成(纯文本, 无工具)  → 无跟进 → stop hooks → 结束
```

- 这就是 agent 循环的引擎: 模型每次用工具都自动触发一次跟进, 直到某轮它不再用工具, 循环自然收敛
- 所谓 agent "多轮自主执行", 机制上就是这个标志位反复翻转 true, 直到变 false

与 turnCount / maxTurns 的关系:

- 每个 follow-up 对应 turnCount+1(轮末 :1679)
- maxTurns 限制的就是跟进次数 —— 超了就 yield max_turns_reached 强制收场(轮末 :1700, abort 时也查 :1508)
- 所以 SDK 里的 maxTurns: 5 = "最多跟进 5 次"

本文"follow-up 主路径"的含义: 3.4. 段反复出现"仅 follow-up 主路径" —— 指这条最常见的 continue 路径(有工具 → 收集 → 下一轮)。中断/错误/恢复那些站点直接 return, 不走轮末收集; 只有跟进路径才需要为下一轮准备增量(4. 段存在的意义)。

### 术语: transition(转移原因)

State 的一个字段, 记录"上一轮迭代为什么 continue"。名字来自状态机: while 循环每轮 continue 是一次状态转移, 这个字段是转移原因的收据:

```
// Why the previous iteration continued. Undefined on first iteration.
// Lets tests assert recovery paths fired without
// inspecting message contents.
transition: Continue | undefined
```

值域 = 全部 7 个 continue 站点各写各的 reason:

| reason | 站点 | 附加字段 |
|---|---|---|
| next_turn | 主路径(:1725, 有工具 → 正常跟进) | — |
| collapse_drain_retry | :1109 | committed(提交了几条折叠) |
| reactive_compact_retry | :1162 | — |
| max_output_tokens_escalate | :1217 | — |
| max_output_tokens_recovery | :1245 | attempt(第几次续写) |
| stop_hook_blocking | :1302 | — |
| token_budget_continuation | :1338 | — |


生命周期: undefined(首轮)→ 每轮 continue 时被覆盖, 只记最近一次。

三个用途:

- 防重 guard —— 下一轮检查 reason, 避免重复同层抢救(如 reason !== 'collapse_drain_retry' 才 drain)
- 可测试 —— 测试断言"恢复路径触发过", 不用翻消息内容
- 语义自文档 —— debug/遥测一眼看出"循环为什么在转"。类比电梯数码管: 不用拆电梯(检查消息), 看一眼就知道刚发生了什么转移

### pendingToolUseSummary: 跨轮接力的后台摘要(:1053 收割)

是什么: 用 Haiku 给"刚完成的工具批次"生成一行人话摘要, 给 SDK 客户端(移动端 UI)做进度展示 —— 给人看的, 不是给模型的。

摘要 prompt 本身是精心设计的(toolUseSummaryGenerator.ts):

```
"Write a short summary label ... It appears as a single-line row
in a mobile app and truncates around 30 characters,
so think git-commit-subject, not sentence."

Examples:
- Searched in auth/
- Fixed NPE in UserService
- Created signup endpoint
```

时序(点火在轮 N 末尾, 收割在轮 N+1):

```
轮 N(轮末, 工具批次完成后 :1469):
  条件四条: emitToolUseSummaries gate + 有 tool_use
            + 未 abort + 非子 agent
            ("subagents don't surface in mobile UI")
  fire generateToolUseSummary(...)   ← Haiku ~1s
       ↑ 调用即执行: promise 是"已在跑的任务的句柄",
         不是"存起来以后再跑的任务"; await 只是取结果
  state = next { pendingToolUseSummary: promise } → 带进轮 N+1

轮 N+1(毫秒级衔接):
  治理 → 流式(5-30s) ← Haiku 在此窗口并行跑完
  流结束(:1053): await → 早已 settled, 零等待收割 → yield 给 SDK
```

- "轮末才开始执行"依然藏得住: 轮 N 结束 fire 与轮 N+1 流式开始几乎零间隙, ~1s 天然落在 5-30s 流式窗口里
- 所有非 follow-up 站点清空它(六个恢复/中断站点全写 undefined)—— 恢复重来的轮没有"上一批工具完成"语义
- 失败容错: .catch(() => null), 收割时 if (summary) 跳过 —— 锦上添花功能, 永不碰主流程

与 memory 预取对比(同为藏延迟, 点火点不同):

| | 点火时刻 | 收割时刻 | 覆盖窗口 |
|---|---|---|---|
| memory 预取 | user turn 开局(循环外) | 某轮轮末(就绪才收) | 整个 turn |
| tool use summary | 轮 N 工具批次完成后 | 轮 N+1 流结束后 | 轮 N+1 流式窗口 |

共同本质: 凡是秒级的辅助调用, 都不许站在主路径上。

### State 重置策略逐站定制(死循环防护的本体)

```
同一个字段 hasAttemptedReactiveCompact:

stop_hook_blocking 站(:1290)     保留
  理由: compact 已跑且没救回来,
  hook 注入 errors 后重试 → 上下文只大不小
  → 还会 413 → 还会触发 hook → …
  (注释记的事故: 烧数千次 API 调用)

token_budget_continuation 站(:1337)  重置 false
  理由: nudge 只加一条小 meta,
  不放大上下文, 再次 413 的风险
  可接受 → 给恢复留满额度
```

- 每个字段的重置语义 = 对应失败模式会不会被这条路径重新触发, 逐站判断, 不是模板

### 413 的两级抢救: collapse_drain 与 reactive_compact

两个 transition 名字即路径描述: collapse_drain(排空折叠暂存)/ reactive_compact(响应式压缩)+ retry(修完重试同一轮)。

前提(1. 治理段的知识): collapse 平时很克制 —— 折叠是**暂存的**(staged), summary 住独立 store, 读时投影, 平时只按需提交保粒度; proactive autocompact 每轮在治理段查阈值, 是**事前预防**。413 意味着预防漏了(或涨太快), 此时两级抢救, **从便宜到贵**:

```
413 到达(withheld 扣留中)
│
├─ 第一级: collapse_drain_retry(:1086)
│    contextCollapse.recoverFromOverflow(messages)
│    把所有【暂存未提交】的折叠立即全部提交 —— drain(排空)
│
│    · 便宜: 不调 API, 只是把已有的 staged summary
│      全部兑现成空间(190k → 150k 这种)
│    · committed > 0 → transition = 'collapse_drain_retry'
│      → continue 回循环开头 → 治理段重跑 → 重新调 API
│
│    · guard: 上轮 reason 已是 collapse_drain_retry
│      → 不再 drain(暂存已空, 再 drain 也是 0)→ 落第二级
│
└─ 第二级: reactive_compact_retry(:1119)
     reactiveCompact.tryReactiveCompact(...)
     现场跑一次全量压缩(compactConversation 四阶段, 见 [§3 compactConversation: 受控重启](03.5-context.md))
│
     · 贵: 一次 API 调用 + 摘要生成
     · 成功 → hasAttemptedReactiveCompact = true
│       transition = 'reactive_compact_retry'
│       → continue(60k 上下文重新调 API)
│
     · hasAttemptedRecovery 已 true → 不再二次 reactive
       (防螺旋: compact→413→又 compact)

两级的失败出口一样: surface + executeStopFailureHooks → return
(不进 stop hooks —— 见下节)
```

名字里的两个对称:

```
proactive autocompact  事前: 治理段每轮查阈值, 未爆先压(1. 第 6 步)
reactive compact       事后: 413 已发生, 被动抢救(3. 恢复链)
```

两个防重标志各管一级 —— 都是"单发"语义:

| 标志 | 挡住的循环 | 放在哪 |
|---|---|---|
| transition.reason === 'collapse_drain_retry' | drain → 413 → 又 drain(暂存已空) | transition(只记最近一轮) |
| hasAttemptedReactiveCompact === true | compact → 413 → 又 compact | State(跨轮存活, stop-hook 路径特意保留) |

transition 的双重作用: 1. 防重(下轮检查 reason 避免重复同层抢救); 2. 可测试 —— 注释原话 "Lets tests assert recovery paths fired without inspecting message contents"。

一句话: **413 的抢救是两级火箭 —— 先把攒着的便宜货全部兑现(drain), 不够再现场造贵的(compact), 每级只点一次火, 都失败才认输报错**。

### 恢复失败不进 stop hooks(死循环机理)

背景: Stop hooks 是什么 —— 一轮自然结束(模型不再调工具)时, query loop 跑 Stop hooks(:1267), 门卫检查这轮工作完不完整。hook 可以返回 blocking error = 不放行: errors 注入消息, continue 重跑模型(stop_hook_blocking 站点)。

死循环机理, 逐步串:

```
前提: 413 发生, 且恢复链失败(drain 没空间, compact 也救不回)
此刻: 最后一条消息 = 错误消息, 模型没产出任何有效响应

假设照常走 stop hooks:
  t1  Stop hook 跑: "turn 结束了, 评估一下"
      → blocking error("没做完, 继续")
  t2  query loop: 注入 errors → continue → 重新调 API
  t3  上下文还是超限(恢复已耗尽)→ 又 413
  t4  hasAttemptedReactiveCompact 已 true
      → 不再恢复 → 错误浮出
  t5  又到 turn 结束 → 又走 stop hooks → 又 blocking
      (hook 每轮注入的 error 文本还在加 token, 越来越糟)
  t6  t2-t5 无限循环, 烧 API 调用
```

所以: 模型没有有效响应的场合, 根本不进 stop hooks:

- 一切 isApiErrorMessage(PTL 恢复耗尽 / rate limit / auth 失败)
- → executeStopFailureHooks(只通知、不拦截的另一类 hook)
- → return, 会话体面结束

本质: Stop hook 的设计前提是"有一段工作可评估"。API 错误时没有工作, hook 只能机械 blocking; blocking 触发重试; 重试必然再失败(病根 —— 超限的上下文 —— 没被除掉); 且每次循环 hook 还在加料。**每一步都符合各自的局部逻辑, 连起来是灾难** —— 典型的"组件全对, 组合出死锁"。修复不是给循环加上限, 而是识别"重试无意义"的前提, 从流程上根本不走那条路。

### max_output 的升级阶梯

- 阶梯 1: 升 64k 同请求重发(零 meta 消息) —— 条件: override 未设 + 无 CLAUDE_CODE_MAX_OUTPUT_TOKENS env
- 阶梯 2: meta user message 续写, 措辞锁死:
  - `"Output token limit hit. Resume directly — no apology, no recap of what you were doing. Pick up mid-thought if that is where the cut happened. Break remaining work into smaller pieces."`
  - 恢复消息本身也是 prompt 工程: 禁止模型把配额花在道歉上
- 限次 MAX_OUTPUT_TOKENS_RECOVERY_LIMIT

### 总纲

- 分派段的本质: 重试也是一种被管理的行为
- 每个 continue 站点都要回答三问:
  - 为什么重试(transition 记因)
  - 已经试过什么(恢复计数 / attempted 标志)
  - 哪些保护位这条路径上不能动(逐站判定的重置策略)
- 普通系统"失败了就重试", 这里是"知道什么时候不该再试"

---
