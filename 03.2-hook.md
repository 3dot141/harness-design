# 3.2 hook

## hook 是什么

用户在设置里配置的事件扩展点:shell 命令,响应系统事件执行。它是动态内容的生产者之一(harness 不知道的用户内容),也是唯一拥有"反向控制权"的机制。与 prompt 组装的关系:两条生产线,汇入同一条注入管道——但 hook 多一样东西:拦的权力。

```
动态内容的生产者
├─ 内置探测器(getAttachments 的 ~25 个)      ← harness 自知的内容
└─ 用户 hook(SessionStart / UserPromptSubmit…) ← harness 不知道的用户内容
     ↓ 同一条 attachment 渲染管道(hook_additional_context 与
       skill_listing 是同一 union 的兄弟类型)
     ↓ <system-reminder> 注入对话流

控制反向流(仅 hook 有):
  PreToolUse → block/allow 工具    Stop → 阻塞结束
  UserPromptSubmit → 作废输入      PreCompact → 参与压缩
```

## 17 个事件全集(coreTypes.ts:25)

```
输入类:  UserPromptSubmit
工具类:  PreToolUse / PostToolUse / PostToolUseFailure
停止类:  Stop / StopFailure
压缩类:  PreCompact / PostCompact
会话类:  SessionStart / SessionEnd / Setup
子代理:  SubagentStart / SubagentStop
权限类:  PermissionRequest / PermissionDenied
其它:    Notification / TeammateIdle
```

## 两种能力:说与拦

| 能力 | 路径 | 实例 |
| --- | --- | --- |
| 说(注入) | additionalContext → attachment → system-reminder | SessionStart/Setup 注入会话底座;UserPromptSubmit 附加内容或改写输入 |
| 拦(控制) | block 决策进入各控制点 | PreToolUse 拦工具;Stop 拦停止;UserPromptSubmit 的 blockingError 作废整条输入(原输入不进上下文, 后续全部不发生) |

- 门卫在装配线之前: block 发生时 attachment 探测器不启动、API 调用不发生
- 注入顺序即话语顺序: hook 的 additionalContext 排在 attachment 批之前 — 用户/插件想说的先到

## 三条咬合

- **通道咬合**:hook 输出与内置动态内容汇入同一 attachment 管道,共享 system-reminder 语义,共享"不打扰 system prompt 缓存"的纪律。
- **协议咬合**:静态 System 段预先声明语义——"Treat feedback from hooks as coming from the user; 被阻塞先调整自己, 不行请用户查配置"。没有这条, hook 输出就是来历不明的噪声。
- **生命周期咬合**:SessionStart 在 startup/resume/clear/compact 四个时机重放——compact 后与 invoked_skills 恢复、CLAUDE.md 重载同批, hook 注入的上下文活过压缩。

## 一条边界

没有任何 hook 事件能改 system prompt。用户想影响宪法,正规通道是 CLAUDE.md(走 userContext)、output style(动态段)、插件规则——都经过 harness 组装管线。hook 只能影响对话流(说)与控制流(拦)。**说与拦分离,注入与控制分离——宪法定义权不下放给事件回调。**

## 设计亮点:先渲染、后等待(useDeferredHookMessages)

```
CLI 启动 → SessionStart hooks 后台跑(~500ms) → REPL 立即渲染(不等)
hook promise resolve → 消息 prepend 到消息流最顶部(会话底座)
用户首次提交 → awaitPendingHooks()(强制 join)
→ 保证: 模型第一条请求里一定含 hook 上下文
```

两个保证拆成两个时刻解决:
- 用户体感: 界面秒开(启动时不等 hook)
- 模型保证: 首请求必含 hook 上下文(首次 API 调用前强制 join)

等待被推迟到最后一个可以推迟的位置(首次 API 调用前), 而不是启动时。
