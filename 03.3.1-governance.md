# 3.3.1 输入治理段

## 1. 输入治理段

### 源码细节

```
每轮迭代, 进模型前(query.ts 行号锚定):

:354  messagesForQuery = getMessagesAfterCompactBoundary(messages)
        └ 界碑之前的旧消息不进模型(纯筛选)
:369  applyToolResultBudget(...)
        └ 超限旧结果 → 占位符; 已见结果冻结(seenIds)
:396  snipCompactIfNeeded(...)         [HISTORY_SNIP]
        └ 剪老消息(纯删除); tokensFreed 传出; yield snip 界碑
:412  microcompact(...)
        └ 时间触发短路 / cached MC(缓存编辑) / no-op
:429  contextCollapse.applyCollapsesIfNeeded(...)
        └ 读时投影; staged → 按需 commit
:447  fullSystemPrompt = appendSystemContext(...)   ← 治理段末尾拼接
:453  autocompact(...)
        └ 兜底; 熔断器; 前五步达标则 no-op
```

### 六步总表

| # | 步骤 | 解决的问题 | 怎么做 | 破坏性 |
|---|---|---|---|---|
| 1 | boundary 截取 | 历史里已有 compact 界碑, 界碑前的旧消息不能进模型 | 找最后一个界碑 slice 之后; HISTORY_SNIP 开启时再叠 projectSnippedView —— 本身是个投影管道 | 零(纯筛选) |
| 2 | tool result budget | 工具结果聚合体积失控(十轮 Read 就是几万 token) | 按工具的 maxResultSizeChars 上限, 超限的旧结果替换成占位符 | 替换内容 |
| 3 | history snip | 老轮次中语义上已无关紧要的整条消息 | 纯删除(不生成 summary); 剪掉 + 插界碑 + 投影; tokensFreed 传给第 6 步 | 剪消息(零 API) |
| 4 | microcompact | 工具结果级压缩(比 6 细) | 三条路径(时间触发短路 / cached MC / no-op) | 视路径而定 |
| 5 | context collapse | 既要减体积又要保粒度、可恢复 | 两阶段(暂存/提交)+ 读时投影 | 投影(不动原件) |
| 6 | autocompact | 以上全不够 | 整段对话 → 单条 summary, 插界碑 | 全量替换 |

### 顺序依赖(注释级因果论证)

```
每轮迭代进入模型前:
1. compact boundary 截取
   └ state.messages 里界碑之前的旧消息不进模型
2. tool result budget(内容替换)
   │ 必须在 4 前:
   │   cached MC 纯按 tool_use_id 操作, 从不看内容
   │   → 内容替换对它不可见 → 二者干净组合
3. history snip
   └ snipTokensFreed ────────────────┐
4. microcompact                      │ 传给
   └ (cached 版: 延迟边界消息,        │
      用 API 实报 token 数, 不用估算)  │
5. context collapse                  ▼
   │ 必须在 6 前:                autocompact
   │   collapse 能降到阈值以下        的阈值判断
   │   → autocompact no-op           (估算看不见
   │   → 保住粒度化上下文,             snip 释放量)
      不做全量 summary
6. autocompact(最后兜底)
```

三条论证:

- A. budget 在 microcompact 前(query.ts:373): `cached MC operates purely by tool_use_id (never inspects content), so content replacement is invisible to it and the two compose cleanly` —— 内容替换改内容、MC 按 id 剪, 两个维度正交所以可叠加
- B. collapse 在 autocompact 前(query.ts:429): `if collapse gets us under the autocompact threshold, autocompact is a no-op and we keep granular context instead of a single summary` —— 保粒度优先, 能用细的不动粗的
- C. snip 的 tokensFreed 是数据依赖(query.ts:396): `tokenCountWithEstimation alone't see it (reads usage from the protected-tail assistant, which survives snip unchanged)` —— 估算读的是受保护尾部 assistant 的 usage, snip 剪的是更早历史, 不传这个数 autocompact 会误判阈值

### 第 1 步: boundary 截取与三道防线

界碑是历史数组里的一条**真实消息**(不是标记位):

```
{ type: 'system', subtype: 'compact_boundary',
  compactMetadata: { trigger, preTokens, userContext,
                     messagesSummarized,
                     preservedSegment{headUuid, anchorUuid, tailUuid} } }
```

- compact 后 query loop 的 state.messages 确实被整体替换成 [界碑, summary, 保留段, ...] —— 运行时数组里本不该有旧消息
- 但有一个入口会重新引入旧消息: **resume**

三道防线(QueryEngine.ts:693 注释原文为证):

```
transcript(盘) ── append-only 全历史日志, 旧消息永远在盘上
     │ resume 加载
     ▼
正常路径: preservedSegment 重链成功
  (tailUuid walk → applyPreservedSegmentRelinks → pruning)
  → 内存数组已被剪
     │
退化路径: 重链失败(子进程被杀在 flush 前/老版本 transcript)
  → "resume loads FULL PRE-COMPACT HISTORY" 旧消息回来了
     │
     ▼
query loop 每轮: getMessagesAfterCompactBoundary(messages)
  → 无论上面哪条路径, 进模型的永远是新世界
```

- 设计要义: **最便宜的一层(slice, 纯筛选零破坏)放最外面兜底**, 不信任上游清理干净
- 三道防线各司其职: 第一层保完整(审计/回退), 第二层省内存, 第三层保正确 —— 发给模型的东西永远干净, 不管前面发生了什么
- HISTORY_SNIP 开启时此步再叠 projectSnippedView 投影 —— 本身是个投影管道

### 第 2 步: tool result budget —— 冻结已见结果

applyToolResultBudget 的状态机(toolResultStorage.ts:924):

- `seenIds`: 凡是发过模型的结果冻结, 永不替换
- 只有还没见过模型的新结果超限时才替换
- resume 时 reconstructContentReplacementState 从 transcript 记录重建状态, 让 budget "make the same choices it made in the original session"
- 落盘条件: querySource 以 agent: / repl_main_thread 开头(会被 resume 读回的才 persist; ephemeral fork 不写)
- fork resume 的 gap-fill: 父态 replacements 补缺

为什么这么较真? 不是为了省 token, 是为了 prompt cache: 同一条消息如果这一轮是原文、下一轮变占位符, 缓存 prefix 就断裂, 整段缓存作废。冻结语义 = 历史形态一经发出就不可变。

### 第 3 步: history snip —— 消息级纯删除

一句话: 不生成 summary, 剪掉就是剪掉。赌"老轮次某些消息语义上已无关紧要" —— 不值得花一次 API 调用去总结, 直接拿掉。零 API、纯本地, 所以排在 microcompact 之前: 它能达标, 后面就不用跑。

```
手段            粒度        剪掉后留下什么       成本
─────────────────────────────────────────────────────
autocompact    全对话      一条大 summary       一次 API 调用
microcompact   工具结果    占位/缓存编辑        近零(缓存编辑)
snip           整条消息    什么都不留(纯删除)   零(纯本地, 无 API)
```

三条触发路径(人机协同, 不只自动):

```
1. 自动     治理段每迭代 snipCompactIfNeeded(messages)
            阈值触发, 无需参与者操心
2. 模型主动  SnipTool(tools.ts:123, HISTORY_SNIP gate)
            模型按 [id:] 引用要剪的消息
3. 用户手动  /force-snip slash 命令(commands.ts:83)
```

"哪些历史没用了"是语义判断, 启发式做不好 —— 设计上外包给最懂的参与方(模型/用户), 自动路径只做兜底。

[id:] 标签机制(messages.ts:1618) —— 模型怎么指定剪哪条:

- 每条非 meta user 消息, 在发给模型的副本尾部注入 `[id:xxxxxx]`
- 6 字符 base36, 从消息 UUID 确定性派生 —— 同一条消息永远同一个 id
- 只改 API-bound 副本, 存储原件不动
- 工具不在列表时不注入 —— 注释: "confuses the model and wastes tokens on every non-meta user message"(白花 token 还误导模型)

配套 nudge(getContextEfficiencyAttachment, attachments.ts:3963):

- shouldNudgeForSnips(messages) 判断上下文效率变差 → 注入 context_efficiency attachment, 提醒模型考虑主动 snip
- gate 必须与 SnipTool.isEnabled() 一致 —— 不往一个不存在的工具方向 nudge

剪掉后的双账本同步(snipReplay):

```
query loop 侧                     QueryEngine 侧(持久 store)
messagesForQuery(治理副本)         mutableMessages(会话原件)
     │ snip 执行                       │
     │ tokensFreed → autocompact        │
     ▼                                 ▼
yield snip 界碑 ─────────────→ snipReplay 收界碑:
                                  在 store 上重放
                                  snipCompactIfNeeded(store, {force:true})
                                  "remove zombie messages" + truncate 控内存
```

- 治理副本剪了, store 不同步剪 → 僵尸消息留在 transcript, resume 时又回来

保护规则(不是什么都剪得):

- protected-tail assistant 不剪(受保护尾部, tokenCountWithEstimation 读它的 usage)
- 带 tool_reference 的消息不剪(toolSearch.ts:539)—— 工具延迟发现机制依赖它们; compact 的做法是把发现集快照到界碑 metadata, snip 的做法更干脆: 这些消息根本不进剪除名单

判定权分布 —— 谁决定"不需要"(三个触发路径 = 三个判定者):

```
1. 模型判定(SnipTool)── 语义判断的主力
   模型看得到全部历史 + 每条消息的 [id:xxxxxx] 标签
   判定依据就是它自己的任务上下文:
   "我在修 bug B, 前面探索 bug A 的那 20 条工具结果已无关"
   → 调 snip 工具按 id 剪 —— 这是语义判断, 启发式做不了

2. 自动判定(snipCompactIfNeeded)── 启发式圈候选 + 负向排除
   从保护名单反推的框架:
   · 候选范围: 老轮次的大块消息(体积 + 年龄维度)
   · 排除名单: protected-tail assistant /
     带 tool_reference 的消息 / 界碑消息

3. 用户判定(/force-snip)
   用户翻 scrollback 觉得"这段别占着" → 强制剪
```

"不需要" ≠ "消失"(投影语义):

- 被剪的消息从【发给模型的视图】消失(projectSnippedView 每轮投影), 但在【历史/transcript】仍在, 界碑标记边界
- 判定"不需要"的粒度是"对模型的工作集不需要", 不是"对系统不需要" —— 审计/UI/回退仍可用

判定为什么保守(错误代价不对称):

```
剪错一条有用消息 → 模型看不到 → 任务可能失败(代价大)
少剪一条        → 多花 token(代价小)
→ 启发式只圈"安全候选"(保护名单宁可多保护)
→ 真动手前靠 nudge(context_efficiency attachment):
   "效率变差了, 考虑 snip" 是提示【模型】来判, 不是自动剪
→ 机械部分管安全, 语义部分管决策
```

各手段"不需要"判定的横切对比:

| 手段 | 判定方式 | 智能度 |
|---|---|---|
| tool result budget | 纯机械(超尺寸 + 未见过模型) | 零 |
| microcompact | 纯机械(时间间隔/计数) | 零 |
| snip | 混合: 启发式圈候选 + 模型语义判定 | 中 |
| collapse | 语义分块(段落级折叠) | 中高 |
| autocompact | 不挑 —— 判定"什么时候动手", 不判定"哪些不要" | 一把梭 |

- 规律: **粒度越细越依赖语义判定, 破坏性越大判定越粗糙** —— 剪错细粒度代价可控(单条), 一把梭不敢挑(挑错丢大局)

SnipTool 的模型判定是"搭便车"(零额外 API):

```
轮 N 轮末: context_efficiency attachment 注入(nudge)
轮 N+1:   主模型正常输出(本来就要调 API 的一轮)
          └ 在正常推理里顺手判断 + tool_use: snip([...ids])
执行:     本地剪除 + 插界碑(零 API)
```

- 判定成本 ≈ 0: 嵌在主模型本来就要跑的一轮里, 和调 Read/Bash 无本质区别
- 与 sideQuery 家族(discovery/memory 检索)的对比:

| | 方式 | 成本 |
|---|---|---|
| snip 判定 | 主对话搭便车(轮内工具调用) | 零额外调用 |
| skill discovery | 专门跑 sideQuery(Haiku+AKI) | 秒级 → 预取+guard |
| memory 相关性 | sideQuery 检索 | 同上 → 预取 |
| tool use summary | sideQuery 生成 | ~1s → 藏进下轮流式 |

- 分界线: **判断所需的信息在不在我(主模型)眼前** —— 在眼前搭便车, 不在就 sideQuery(成本决定通道家族)

### 第 3 步的缓存账: snip 破缓存, 为什么仍然做

snip 破缓存 —— 剪除点之后全部重算:

```
缓存前缀:  [A][B][C][D][E][F][G] ← 已缓存
snip 剪 C:
下轮请求:  [A][B][D'][E][F][G](D' = snip 界碑)
                ↑ 从 C 原位置起后面全部错位
→ D 之后按 cache_write 重算(1.25x)
```

三笔账:

```
账 1 一次性 vs 持续性:
     破缓存是一次性的(重算后新前缀又缓存了)
     不治理是持续的(上下文每轮超重, 更贵甚至 413)
     → 用一次性的代价买持续的空间, 划算才做

账 2 时机控制 —— 不到压力不动手:
     六步每步 if needed, 不到阈值 no-op(本地估算, 缓存无恙)
     nudge(context_efficiency)本身就是"效率账算不过来"的信号

账 3 与替代方案比(同样减 30k):
     snip        破缓存一次 + 零 API
     autocompact 破缓存一次 + 一次 API + 20k 输出 + 信息全损
     413 后抢救  reactive compact(更贵)+ 惊吓用户
```

治理六步的真实排序逻辑 —— 按信息破坏性递增, 不是缓存代价:

| 步骤 | 信息代价 | 缓存代价 | API |
|---|---|---|---|
| 1 boundary | 零 | 零(视图本就稳定) | 零 |
| 2 budget | 替换占位 | **零**(冻结已见, 精心设计的不破) | 零 |
| 3 snip | 纯删除(不可逆) | **破(一次性)** | 零 |
| 4 microcompact | 清数据 | 时间触发=破(缓存已死, 免费)/ cached=**不破** | 近零 |
| 5 collapse | 折叠(可恢复) | **破(一次性)** | spawn 时已付 |
| 6 autocompact | 全量 summary | 破 | 一次调用 |

两个流派:

- **不破派**(精心设计): budget 的 seenIds 冻结(新结果本来就不在缓存里, 替换不破)、cached MC 的缓存编辑(服务端原地删) —— 它们的存在本身说明"不破缓存"被当成值得专门设计的目标
- **破派**(时机哲学): snip/collapse/时间触发 MC 都破, 但被约束在"值得破的时刻"(阈值逼近/缓存已死/语义判定无关)动手 —— 时间触发 MC 的注释是这派的方法论: "缓存反正已经过期, 重写不可避免, 所以现在做破坏性动作是免费的"

决策规则一句话: **零压力时一切 no-op(缓存无恙)→ 有压力先用不破的手段(budget/cached MC)→ 不够就付一次性缓存代价换持续空间(snip/collapse)→ 极限才付全价(autocompact)**。破缓存不是禁忌, 是明码标价的成本, 在正确的时机花。

### 第 4 步: microcompact 三条路径

microcompact = **工具结果级的微型压缩** —— 只清理历史里的 tool_result 原始数据, 对话叙事原样不动:

```
压缩前:
  [user: 帮我修 bug]
  [assistant: 我先读一下文件 (thinking + tool_use: Read)]
  [tool_result: ████ 2000 行文件原文 ████]    ← 膨胀主因
  [assistant: 找到了, 问题在 148 行]
  [tool_use: Edit]  [tool_result: ok]

microcompact 后(叙事全保留, 只清数据):
  [user: 帮我修 bug]
  [assistant: 我先读一下文件 (...)]             ← 保留
  [tool_result: <早期大结果已清理>]             ← 只动这里
  [assistant: 找到了, 问题在 148 行]            ← 保留! 这是叙事
  ...
```

"micro" 的三个维度:

| 维度 | microcompact | autocompact |
|---|---|---|
| 粒度 | 单个 tool_result(按 tool_use_id 定位) | 整段对话 |
| 影响 | 叙事零损伤 —— 推理/指令/结论都在, 只少原始数据 | 结构重建, 一切变 summary |
| 成本 | cached MC 走缓存编辑, 不破前缀, 近零 | 一次 API 调用 + 20k 输出, 破缓存 |

为什么存在: 工具结果是上下文膨胀的主因(一个 Read 2000 行, 十轮几万 token), 但价值密度最低 —— 看过用过之后, 原始数据不再需要(结论已写进 assistant 叙述)。清"原始数据"保"叙事"是最便宜的减重 —— 排在六步第 4 位、autocompact 之前: **能用 micro 解决就不动 macro**。

```
入口 microcompactMessages()
│
├─ 1. 时间触发, 短路返回
│    距上一条 assistant 消息间隔超阈值
│    → 服务端缓存必然已过期, prefix 反正要重写
│    → 趁机把旧工具结果直接清掉("缓存都没了, 不留情面")
│    → 同时跳过 2.(2.假设暖缓存, 而刚确认是冷的)
│
├─ 2. cached MC(缓存编辑 API)
│    用 cache_edits 在 API 层删 tool_result, 不失效缓存 prefix
│    · 不改本地消息(cache_reference/cache_edits 在 API 层加)
│    · 只在主线程跑 — forked agent 不许注册进全局状态,
│      否则主线程会试图删自己会话里不存在的 tool
│    · 延迟边界消息: 等 API 实报删除量(累计-基线), 不用估算
│
└─ 3. 都不可用(外部构建/非 ant/不支持模型/子 agent)
     → no-op, 交给第 6 步 autocompact
```

1.的推理值得单独看: "缓存反正已经过期, 重写不可避免, 所以现在做破坏性动作是免费的" —— 破坏性动作的成本不是固定的, 是相对于缓存状态的。

什么叫缓存编辑(cache editing):

```
问题 —— 常规删除必然破缓存(前缀匹配原理):
  上次(已缓存):   [A][B][C][D][E]
  删 B 后重发:     [A][C][D][E]
                    ↑ 从 B 起后面全部错位 → 缓存作废
                    → C D E 按 cache_write 重算(1.25x, 贵)

缓存编辑 —— 服务端原地改:
  请求带指令: cache_reference("接着我上次的缓存")
             + cache_edits([删除 tool_result xxx, ...])
  → 服务端在缓存副本里【原地删除】这些区间
  → 编辑点之前的部分继续命中(0.1x 的 cache read)
```

- 类比: 书架上抽掉一本册子 vs 整面墙重排 —— 缓存编辑抽一本, 常规删除从抽掉位置起全部错位重排
- 把"删除"从客户端重写前缀(必断缓存)变成服务端原地修改(编辑点前仍命中) —— 是"删除历史"与"保住缓存"两个矛盾诉求的唯一调和
- 三个配套:
  · **本地消息不动** —— cache_reference/cache_edits 是随请求附加的指令, transcript/REPL 里 tool_result 原样("operates purely by tool_use_id, never inspects content"的完整含义)
  · **实报删除量** —— API 在 usage 里返回 cache_deleted_input_tokens(累计值, 客户端减基线拿增量) —— 延迟边界消息等响应后才 yield, 不信估算
  · **条件苛刻** —— 主线程 only(forked agent 混进全局状态会删错)+ 模型支持 + feature gate —— 与 API 深度耦合的实验能力

### 第 5 步: context collapse —— 段落级折叠

context collapse = **段落(span)级折叠** —— 把连续的老消息段收拢成一条小 summary, 原段从模型视野退出, 但原件保留、可恢复。"折叠"是精确的用词: 像手风琴 —— **收起来, 不是扔掉**:

```
原始历史(模型视野):
  [user: 调查一下性能问题]
  [10 条探索消息: 读 profiler 输出 / 试了三个假设 / ...]
  [assistant: 定位到 N+1 查询]
  [5 条修复消息]

collapse 后(模型视野):
  [user: 调查一下性能问题]
  [collapsed span: 探索了 profiler 输出, 排除缓存与锁
                   假设, 确认 N+1 查询]  ← 10 条 → 1 条
  [assistant: 定位到 N+1 查询]            ← 结论性消息保留!
  [5 条修复消息]
```

- 对比 autocompact 同场景: 全部变成一条大 summary(修复过程也进去) —— collapse 保留多条小 summary, 每段各自可读、结论原样站在外面
- 独特卖点 = **保粒度的压缩**: 压缩了体积, 但每段的历史语义各自成立
- 为什么平时只 stage 不全 commit: commit 了那段就真从视野退出; stage 还能反悔 —— 上下文没压力时保留原文更好, **溢出(drain)才值得全部兑现**
- 为什么读时投影: 原件和视图分离 —— UI 完整、审计完整、模型看瘦身版; 跨 turn 稳定(projectView 幂等重放)
- 为什么排在 autocompact 前: collapse 能把体积降到阈值以下, autocompact 就 no-op —— **保住 N 条小 summary, 不被迫一把梭**

在治理谱系里的位置:

```
tool result 级   microcompact   清数据保叙事(单条结果)
消息级           snip           剪整条(语义判定, 零摘要)
段落级           collapse       连续段折叠成小 summary ★
全对话级         autocompact    一把梭大 summary(兜底)
```

什么时候触发 stage / commit / drain(三档急迫度):

```
stage(暂存)── 后台时机
  stats 里有 stagedSpans 计数 + totalEmptySpawns
  ("empty spawn" = 生成了空 summary 的健康度警告)
  → "spawn" 说明折叠 summary 的生成是后台派生任务
  → 算好折叠方案 + 生成 summary, 存进 collapse store
    但不生效 —— 模型视野不变

commit(提交)── 治理段每轮, 按需
  applyCollapsesIfNeeded 注释原文:
  "maybe commit more collapses"
  → 每轮进模型前: 投影当前视图 → 判断体积
    超阈值 → 把更多 staged 兑现(写 commit log)
  → 目标(注释原文): "if collapse gets us under the
    autocompact threshold, autocompact is a no-op"
    —— 兑现到"够"为止, 不多兑现

drain(兑现全部)── 413 时
  recoverFromOverflow(3.3.3 第 413 抢救的第一级)
```

- 本质: **平时攒着不生效(stage)→ 逼近阈值兑现一部分(commit, 够用就停)→ 真溢出全兑现(drain)** —— 兑现有代价(那段从视野退出), 永远兑现到"刚好够"为止
- 调度形态: 事件驱动(启动时 initContextCollapse 注册 + subscribe 发布订阅 + spawn 派生), 不是后台线程轮询 —— 上下文只在轮末变大, 事件点触发零浪费
- 精确的 stage 调度代码在 vendored 剥离区(CONTEXT_COLLAPSE 的 DCE 边界, 见 3.4 的"gate 三层开关体系")

与 SessionMemory 同族(生成贵的东西放平时, 兑现放在需要时):

| | 平时攒 | 用时兑现 |
|---|---|---|
| SessionMemory | 每 5k token 萃取 notes | compact 时替代全量压缩 |
| collapse | 后台 spawn 折叠 summary(stage) | 超阈值 commit / 413 drain |
| memory 预取 | turn 开局点火 | 轮末就绪才收 |

机制(两阶段 + 投影):

```
stage(暂存)              commit(提交)              drain(紧急)
折叠 span 计算 summary    平时按需提交               溢出(413)时
→ 暂存, 不进 REPL 数组    → 写入 commit log          recoverFromOverflow
   summary 住 collapse      ↓                         把所有 staged
   store(独立存储)        projectView()              立即提交换空间
                            每次进 query 重放 log      (第一层恢复, 便宜)
                            投影成视图
```

- REPL 的全历史原件不动, 模型看到的是投影视图 → 跨 turn 稳定(projectView 每次重放, 幂等)
- turn 内投影结果通过 state.messages 前流(query.ts:1192), 下轮 projectView no-op
- 放在第 6 步前的理由: collapse 达标 → autocompact no-op → 保住 N 条小 summary, 而不是一把梭成单条大 summary

### 第 6 步: autocompact 兜底 + 熔断器

```
autoCompact.ts:241:

入口 autoCompactIfNeeded(messages, ..., snipTokensFreed)
│
├─ DISABLE_COMPACT env? ── 紧急开关, 直接返回 no-op
│
├─ 熔断器: consecutiveFailures
│    ≥ MAX_CONSECUTIVE_AUTOCOMPACT_FAILURES?
│    → 停手。注释原文: "sessions where context is
│      irrecoverably over the limit hammer the API
│      with doomed compaction attempts on every turn"
│      (无可救药超限的会话, 不许每轮拿注定失败的
│       压缩锤 API)
│
├─ shouldAutoCompact(messages, model, querySource,
│                     snipTokensFreed)
│    ↑ 第 3 步的数据依赖落地处(估算看不见 snip 释放量)
│    未到阈值 → no-op 返回
│
├─ 内部还有一层优先级:
│    ① 先试 session memory compaction(实验路径)
│       成功 → 用 notes 替代, return
│    ② 不行再 compactConversation(全量)
│
└─ compact 成功后:
     tracking.turnCounter 归零
     ("reflect the MOST RECENT compact" —— 界碑后的
      轮数从新世界起算)
```

深挖引用:
- compactConversation(② 全量路径)的内部 —— 四阶段/恢复清单/受控重启: [§3 compactConversation: 受控重启](03.5-context.md)
- session memory compaction(① 实验路径)的双链机制: [§5 SessionMemory: 平时攒, 压缩时用](03.5-context.md)
- 熔断数字与阈值(20k/13k/3): [§2 预算阈值全景表](03.5-context.md)

### 频次分层

| 动作 | 频次 | 为什么 |
|---|---|---|
| memory 预取 | 每 user turn 1 次 | prompt 在迭代间不变, 每迭代触发 = 问 sideQuery 同一问题 N 次 |
| config 快照 | 每 query 1 次 | env/statsig 入口拍一次, feature() gate 故意排除 |
| 治理六步 | 每迭代 | 每轮工具结果都改变上下文体积, 进模型前必须重检 |
| skill 预取 | 每迭代点火 + guard | 非写迭代早退 |
| refreshTools | 每轮递归前 | 新连的 MCP server 下一轮可用 |

### 治理段末尾的两个 prompt 拼接时点

- `fullSystemPrompt = appendSystemContext(systemPrompt, systemContext)`(query.ts:447)
- 进 callModel 前 `prependUserContext(…, userContext)`(query.ts:665)
- 即四条注入通道里 userContext / systemContext 的实际拼接时点在这里: 治理完、进流式段之前

### 总纲: 整条管线围着 prompt cache 转

每一步都在回答同一个问题 —— "这个动作会不会破缓存? 破不起就换一种不破的做法, 或者等到破得起的时候再做":

- budget → 冻结已见结果(cache 稳定)
- microcompact 1. → 只在缓存已死时做重清(破坏免费时才破坏)
- microcompact 2. → 缓存编辑 API, 删内容不破 prefix
- collapse → 读时投影, 原件不动
- autocompact → 真会破(界碑后全新 prefix), 所以排最后, 前五步能达标它就 no-op

这与 prompt 部分的 2^N 碎片化一脉相承: 静态区防前缀碎片, 治理段防历史变形 —— 上下文的形态稳定性是贯穿两个部件的同一个设计目标。

---
