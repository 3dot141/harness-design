# 3.5 上下文治理

## 定位

- "信息越多, 系统越聪明"是危险神话。代理系统不是图书馆, **上下文不是"存进去就算拥有"的仓库, 它首先是一笔昂贵、易膨胀、会自我污染的预算**
- 本章回答: Claude Code 怎样防止自己被记住的东西拖死 —— "记住更多"和"治理记忆"是两种制度
- 全景: 上下文治理是一个**三层制度**, 按寿命分层, 越往下越频繁:

```
┌────────────────────────────────────────────────────┐
│ 指令层  CLAUDE.md 分层加载                          │
│   寿命: 跨项目(Managed/User)/ 跨会话(Project/Local) │
│   变化: 跟文件走, 人写人维护                         │
├────────────────────────────────────────────────────┤
│ 记忆层  MEMORY.md 索引制(跨会话) + SessionMemory    │
│   寿命: 跨会话 / 会话内                              │
│   变化: 模型写, 两步协议 + 预算硬限                   │
├────────────────────────────────────────────────────┤
│ 运行时层  治理六步管线 + compact 受控重启             │
│   寿命: 每轮迭代 / 每次压缩                          │
│   变化: 运行时自动(见 3.3.1 输入治理段)                                    │
└────────────────────────────────────────────────────┘
   越往上越稳定(人维护), 越往下越频繁(机器维护)
   每层都有自己的预算约束(见 2. 阈值表)
```

---

## 1. CLAUDE.md: 长期指令的分层加载

### 四层与装配顺序(claudemd.ts 头注释 + :804-940)

| 层 | 来源 | 开关 | 语义 |
|---|---|---|---|
| Managed | /etc/claude-code/CLAUDE.md + managed rules 目录 | always loaded | 组织 policy, 不可关 |
| User | ~/.claude/CLAUDE.md + ~/.claude/rules/*.md | userSettings | 个人全局 |
| Project | 逐目录: CLAUDE.md、.claude/CLAUDE.md、.claude/rules/*.md | projectSettings | checked-in, 团队共享 |
| Local | 逐目录 CLAUDE.local.md | localSettings | 私有(gitignored) |

```
装配顺序 = 优先级(头注释原文):
"Files are loaded in reverse order of priority, i.e. the latest
 files are highest priority with the model paying more attention"

Managed → User → [root ──→ ... ──→ CWD 逐目录]
                                ↑ dirs.reverse(): 从根向 CWD
                                离 CWD 越近加载越晚
                                = 越靠近注意力前沿
(Local 晚于同目录 Project → 私有覆盖团队)
```

### @include 白名单与规则

```
TEXT_FILE_EXTENSIONS: md/txt/text + json/yaml/yml/toml/xml/csv + html...
注释: "prevents binary files (images, PDFs, etc.)
        from being loaded into memory"

· 语法 @path / @./rel / @~/home / @/abs(无前缀 = 相对)
· 只在 leaf 文本节点生效(不在代码块内 —— 防 include 注入)
· 被包含文件作为独立条目加在包含文件【之前】
· processedPaths 防循环引用; 不存在的文件静默忽略
```

### 三个细节

- **nested worktree 去重(issue #29599)**: worktree 嵌在主仓内时向上遍历会同时穿过两个 root, checked-in 文件加载两次 → skipProject 跳过主仓侧的 Project 型文件; CLAUDE.local.md 不跳(gitignored 只在主仓存在, "worktree already has its own checkout")
- **external include 的层间差异**: Project 层 @include 仓库外文件需审批(hasClaudeMdExternalIncludesApproved); User 层永远允许 —— 防 checked-in 文件把任意路径内容带进所有人的 prompt
- **条件规则**: managed/user 层 rules 可带 frontmatter 条件, 按目标路径匹配才注入(与 skills 的 paths 条件激活同族)

### 与 [3.1 prompt](03.1-prompt.md) 的衔接

- 装配结果经 getUserContext 注入 userContext 通道, memoize 会话级(见 [3.1 prompt](03.1-prompt.md))
- InstructionsLoaded hooks 在加载完成时通知(带 LoadReason/MemoryType)

---

## 2. 预算阈值全景表

每层制度都有自己的硬预算 —— 散在四个文件里的数字收进一张表:

| 常量 | 值 | 管哪层 | 源 |
|---|---|---|---|
| MAX_ENTRYPOINT_LINES | 200 | MEMORY.md 入口行数 | memdir.ts |
| MAX_ENTRYPOINT_BYTES | 25_000 | MEMORY.md 入口字节 | memdir.ts |
| minimumMessageTokensToInit | 10_000 | SessionMemory 初始化门槛 | sessionMemoryUtils.ts |
| minimumTokensBetweenUpdate | 5_000 | SessionMemory 更新间隔 | sessionMemoryUtils.ts |
| MAX_SECTION_LENGTH | 2_000 | SessionMemory 单节 | SessionMemory/prompts.ts |
| MAX_TOTAL_SESSION_MEMORY_TOKENS | 12_000 | SessionMemory 总额 | SessionMemory/prompts.ts |
| MAX_OUTPUT_TOKENS_FOR_SUMMARY | 20_000 | compact 摘要输出预留 | autoCompact.ts |
| AUTOCOMPACT_BUFFER_TOKENS | 13_000 | autocompact 警戒缓冲 | autoCompact.ts |
| MAX_CONSECUTIVE_AUTOCOMPACT_FAILURES | 3 | compact 熔断 | autoCompact.ts |
| POST_COMPACT_MAX_FILES_TO_RESTORE | 5 | 文件恢复个数 | compact.ts |
| POST_COMPACT_TOKEN_BUDGET | 50_000 | 文件恢复总额 | compact.ts |
| POST_COMPACT_MAX_TOKENS_PER_FILE | 5_000 | 单文件 | compact.ts |
| POST_COMPACT_MAX_TOKENS_PER_SKILL | 5_000 | 单 skill | compact.ts |
| POST_COMPACT_SKILLS_TOKEN_BUDGET | 25_000 | skills 恢复总额 | compact.ts |
| MAX_PTL_RETRIES | 3 | compact 自爆重试 | compact.ts |

阈值判定的顺序也有讲究(autoCompact.ts):

```
getEffectiveContextWindowSize()
  = 模型窗口 - MAX_OUTPUT_TOKENS_FOR_SUMMARY(20k)
    ↑ 先假定 compact 本身要花钱, 不把窗口吃到只剩一口气
getAutoCompactThreshold()
  = 有效窗口 - AUTOCOMPACT_BUFFER_TOKENS(13k)
    ↑ 再扣警戒缓冲(警告阈值/错误阈值/手动 compact 各自分 buffer)
```

不留余地的系统平时看着节俭, 出事时才暴露 —— 不过是把风险账单留给了下一轮。

---

## 3. compactConversation: 受控重启

### 四阶段主流程(compact.ts:387)

```
阶段 1  前置(hook 层)
  preCompactTokenCount = tokenCountWithEstimation(messages)  ← 先记账
  PreCompact hooks(trigger: 'auto' | 'manual')
    └ hook 可注入 customInstructions(mergeHookInstructions 合并)

阶段 2  摘要循环 for(;;)
  输入清洗: stripImagesFromMessages(
              stripReinjectedAttachments(
                [...boundary 后消息, summaryRequest]))
  └ 图片/文档 → [image]/[document] 占位
    反正会重注入的 attachment 先剥掉(不浪费摘要 token)

  摘要 agent 的配置:
  · systemPrompt: "You are a helpful AI assistant tasked
    with summarizing conversations."
  · ★ 带工具: FileReadTool + ToolSearchTool + MCP 工具
    —— 摘要 agent 可以读文件核实, 不是纯文本压缩
  · thinking disabled; 输出上限 min(20k, 模型上限)

  PTL 自爆处理(救火工具自己也会爆):
  summary 以 PROMPT_TOO_LONG_ERROR_MESSAGE 开头
  → truncateHeadForPTLRetry: 截掉最老的 API-round 组重试
  → 限 MAX_PTL_RETRIES = 3; 截不动 → failed + throw
  compact 的 abort: abortController 传给 forked agent,
  处理 APIUserAbortError —— 防"被 Esc 的 compact"误记成一次成功摘要

  ★ truncated 必须同时穿两处: messages 参数 +
    retryCacheSafeParams.forkContextMessages
    (forked 路径读后者不读前者)

阶段 3  状态清理
  preCompactReadFileState = cacheToObject(readFileState)  ← 先存
  readFileState.clear(); loadedNestedMemoryPaths.clear()
  ★ sentSkillNames 故意不重置 —— 注释原文:
    "re-injecting the full skill_listing (~4K tokens) post-compact
     is pure cache_creation with marginal benefit"

阶段 4  重建(并行恢复)
  Promise.all([文件恢复, async agent 恢复])
  + plan / plan_mode / invoked_skills attachment
```

### 阶段 4 恢复清单(逐项源码验证)

| 恢复项 | 预算 | 关键设计 |
|---|---|---|
| 最近文件(≤5) | 每文件 5k, 总 50k | diff-against-preserved(preserved tail 已有的跳过, 省至多 25k/次) |
| invoked skills | 每 skill 5k, 总 25k | invokedAt 最近优先; per-skill 截断保头部("keeps the head of each file... rather than dropping whole skills"); 预算压力自动丢最不相关; 不跨 agent 泄漏 |
| plan 文件 | — | plan 存在则补 |
| plan_mode 指令 | — | mode==='plan' 时注入(平时只随工具轮注入, 不补则模型忘了自己在 plan 纪律里) |
| agent listing delta | — | **compact.ts 显式重宣告**("after compaction eats prior deltas") |
| deferred tools / MCP delta | — | 随 25 探测器每轮重算 |
| SessionStart hooks(source='compact') | — | 产物进 hookResults |
| PostCompact hooks | — | trigger + compactSummary; 产出显示消息 |
| boundary message | — | compactMetadata{trigger, preTokens, userContext, messagesSummarized} + preservedSegment 重链标注 |
| 会话元数据 | — | reAppendSessionMetadata(保 16KB tail window, resume 显示用户标题) |

统一装配顺序(buildPostCompactMessages, 三条 compact 路径共用):

```
boundaryMarker → summaryMessages → messagesToKeep
→ attachments → hookResults
```

### "在不在"怎么判断: 恢复的依据在上下文之外

每项恢复的存在性判断源各不相同 —— 全部是**消息历史之外的运行时状态**:

```
恢复项            在不在, 问谁?                 谁在维护这本账
──────────────────────────────────────────────────────────────
最近文件          readFileState 非空            工具执行时累计
                  (按 timestamp 排序取 5)        (Read/Write/Edit 写入)
plan 文件         getPlan(agentId) !== null     plan 模式机制
plan_mode 指令    toolPermissionContext          权限模式状态
                  .mode === 'plan'
invoked skills    getInvokedSkillsForAgent      SkillTool 调用时登记
                  (agentId).size > 0             (3.1 的 addInvokedSkill)
agent listing     agentDefinitions.activeAgents 会话装配时
```

- **重启的是消息历史, 不是运行时状态**: 登记簿(ToolUseContext/appState 上的)不参与压缩, 是重建的依据 —— 类比进程重启靠持久化元数据恢复, 不是靠内存里的堆
- 反面推论: 只存在于消息历史里、没有登记簿的状态, compact 恢复不了 —— 只能靠 delta 探测器每轮重算
- **readFileState 的反转: 先清、再重读** —— 登记簿提供的是地址(哪些文件重要), 不是内容; 恢复走 FileReadTool 重读("get fresh content with proper validation"), 压缩期间文件可能已变, 旧缓存不可信
- 通用检验: **凡是怕丢的状态, 都要有消息历史之外的账本**

### agent listing delta 的故事(2^N 碎片化的具体解法)

```
事故: agent 列表住在 tools 前缀里
  请求 = [system 前缀] [tools 前缀] [messages]
  AgentTool.description 里嵌"当前可用 agent 列表" ← 动态
  让列表变的三件事(全是日常操作):
  · MCP 异步连接完成 → 多出 mcp__server 型 agent
  · /reload-plugins → 插件 agent 增减
  · 权限模式切换 → allowedAgentTypes 变
  每次变化: 列表变 → description 变 → tools 段变
  → 缓存前缀断裂 → 全部工具 schema 重算 cache_creation
  注释的账: 这一件事吃掉 fleet cache_creation 的 10.2%
```

```
解法: 搬家 + 增量
  修复前: [tools: AgentTool.description 含动态列表]  ← 缓存断点
  修复后: [tools: description 恒定]
         [messages 尾部: agent_listing_delta attachment]
          ↑ 只在变化时追加 { addedTypes, removedTypes }
            从 transcript 的 prior deltas 重建"已宣告集合",
            与当前集合 diff —— 有 diff 才发, 没变化零开销
```

- 与 [3.1 prompt](03.1-prompt.md) 的 2^N 同一个原则的两处应用: 会变的东西不许住在缓存前缀里(3.1 是界碑前, 这里是 tools 段)—— 要活在尾部, 以增量出现
- **代价链闭环**: delta 住在消息流里 → compact 吃掉 prior deltas → 所以 agent_listing_delta 专门 export 给 compact.ts, 压缩后显式重宣告全量 —— 住前缀缓存常炸(10.2%), 住尾部前缀稳但要接受 compact 时的一次性重播

### 其余设计点

- **摘要 agent 有工具**: compact 不是缩写聊天记录, 是一个带只读工具的小 agent 做核实式摘要, 自己也过权限/治理(querySource: 'compact')
- **恢复动作先问"这笔 cache_creation 值不值"**: sentSkillNames 不重置(省 4k listing)+ 文件恢复跳过 preserved 已有(省 25k)
- **工程决策带数据**: promptCacheSharing 注释记着 2026 年 1 月实验 —— false 路径 98% cache miss, 占 fleet cache_creation 0.76%(~38B tok/day), 集中在 ephemeral 环境; gate 留作 kill-switch
- **auto compact 失败静默**: 只有手动 /compact 失败才弹通知 —— "Auto-compact failures are retried on the next turn and the notification is confusing when compaction eventually succeeds"

### 定性

compact = **受控重启**: 摘要只是中间产物, 真正的产品是"重新铺平的、还能继续干活的工作底座"。

---

## 4. SessionMemory: 平时攒, 压缩时用

### 九栏目模板(prompts.ts 原文, 斜体为每节自带的填写指引)

| 节 | 填写指引(原文要义) |
|---|---|
| Session Title | 5-10 词的描述性标题, 信息密集, 无废话 |
| Current State | 现在正在做什么? 未完成的任务? 紧接着的下一步? |
| Task specification | 用户要求构建什么? 设计决策与解释性上下文 |
| Files and Functions | 重要文件有哪些? 内容是什么、为何相关? |
| Workflow | 通常跑哪些命令、什么顺序? 输出不显然时怎么解读? |
| Errors & Corrections | 遇到过什么错、怎么修的; 用户纠正过什么; **哪些路径失败过不该再试** |
| Codebase and System Documentation | 重要系统组件; 它们如何工作/组合 |
| Learnings | 什么有效/什么无效/避免什么(不与其他节重复) |
| Key results | 用户要的具体输出(答案/表格/文档)**原样完整重复** |
| Worklog | 每步尝试/完成了什么, 每步一句极简摘要 |

- 不是给人抒情的: 关心的是"做到哪了、踩过什么坑、改过哪些文件、下一步接什么" —— 萃取成**可继续工作的操作说明书**, 不求复刻对话

### 更新约束(prompts.ts getDefaultUpdatePrompt 原文要义)

```
开场即声明: "This message and these instructions are NOT part of
the actual user conversation. Do NOT include any references to
'note-taking', 'session notes extraction'..."
  → 提炼是隐形的, notes 内容里不得出现它的痕迹

· ONLY task: 用 Edit 工具更新 notes 文件, 然后停
  (多个 Edit 并行放在单条消息里; 不调其他工具)
· 结构锁死(CRITICAL RULES):
  -- NEVER 修改/删除/新增 section headers
  -- NEVER 动斜体描述行 —— "they are TEMPLATE INSTRUCTIONS
     that must be preserved exactly as-is"
  -- 只更新描述行下方的内容
· 无实质新知可跳过该节 —— 不写 "No info yet" 之类的 filler
· "Write DETAILED, INFO-DENSE content" —— 要具体到文件路径/
  函数名/错误消息/精确命令
· Key results 要完整原样(整张表、整个答案)
```

### 两套阈值是不同层(触发侧与预算侧)

```
触发侧(什么时候提炼)              预算侧(提炼结果多大)
sessionMemoryUtils.ts:33          prompts.ts:8-9
minimumMessageTokensToInit: 10k   MAX_SECTION_LENGTH = 2000
minimumTokensBetweenUpdate: 5k    MAX_TOTAL = 12000
工具调用次数门槛                    九栏目固定模板
```

### 双门槛触发(sessionMemory.ts:134-181)

```
提炼条件 = (token 门槛 AND 工具调用门槛)
         OR (token 门槛 AND 上轮无工具调用)
                ↑ "自然对话断点"分支: 纯文本轮 = 天然安全点

★ token 门槛永远必须 —— 注释原文:
  "the token threshold is ALWAYS required... This prevents
   excessive extractions."
```

### 双链结构

```
链 1  背景持续提炼(攒素材)
  挂点: 主线程轮末 hook, sequential() 防重入
  条件: 只主线程 → ant-only gate → 双门槛
  执行: runForkedAgent 白名单提炼
        createMemoryFileCanUseTool:
          只许 Edit 那一个 memory 文件, 其他全 deny
          (权限最小化的教科书示例 —— 函数级定制 CanUseToolFn)
  产物: notes 文件持续更新(12k 预算内)

链 2  compact 时的优先路径(消费)
  手动 /compact 或 autocompact 触发
  → trySessionMemoryCompaction:
    等 in-progress 提炼完成(带超时)
    notes 不存在/空模板 → return null(落回 3. legacy 全量)
    有货 → 按 lastSummarizedMessageId 切分:
      已提炼的旧消息 → 用 notes 文件替代(不现场压缩)
      之后的消息     → 原样保留(不切断 tool_use/result 配对)
    三个 fallback: 边界 id 找不到(消息被改)→ legacy;
      resumed 无标记 → 只插 summary 不丢消息;
      滤掉旧界碑(防二次 prune 丢新 summary)

配套细节:
· 提炼成功后 updateLastSummarizedMessageIdIfSafe:
  只在"上轮无工具调用"时更新标记
  (防孤儿 tool_result —— 有工具的轮不能作为提炼边界)
· 后台提炼的等待/过期: 15s wait timeout /
  60s stale threshold —— 超时不算失败, 主流程不等它
```

- **本质: SessionMemory 是"提前慢慢写的 summary"** —— 平时每过 5k token 在自然断点萃取一次; compact 时刻到了素材已在手, 已萃取的历史直接用 notes 替代, 免掉一次昂贵压缩。notes 没攒起来也不亏, 落回老路
- 与 [3.3 query loop](03.3-query-loop.md) 的 withheld/预取同族: 把贵的动作拆成便宜的分步, 藏在平时

### 预算自愈(prompts.ts:168-185)

超 12k 时的注入是逐节点名 + 优先级指引:

```
· 逐节列出: "xxx" is ~N tokens (limit: 2000)
· "Aggressively shorten... by removing less important details,
   merging related items, and summarizing older entries"
· "Prioritize keeping 'Current State' and
   'Errors & Corrections' accurate and detailed."
```

### 记忆层速览: MEMORY.md 体系存什么(存储视角)

物理布局: 入口 + 正文两分离:

```
memdir(~/.claude/memories/)
├── MEMORY.md          入口/索引: 每条记忆【一行指针】
│                      硬限: ≤200 行 / ≤25KB, 超限 truncate + 警告
├── user-xxx.md        正文: 独立文件, 带 frontmatter
├── feedback-xxx.md
├── project-xxx.md
└── reference-xxx.md
```

保存永远是两步: 正文写独立文件 → MEMORY.md 加一行指针。入口负责低成本寻址, 正文负责高密度承载。

四类封闭分类(memoryTypes.ts, 每类四要素: description / when_to_save / how_to_use / examples):

| 类型 | 存什么 | 何时存 | 关键设计 |
|---|---|---|---|
| user | 用户画像: 角色、目标、职责、知识 | 学到用户身份/偏好/知识背景的任何时刻 | 目的是 tailor 行为(对资深工程师和对新手解释方式不同); **避免负面评判、避免与工作无关的记忆** |
| feedback | 用户对做事方式的**纠正和确认** | 用户说 "no not that / don't" **或** "yes exactly / keep doing that" | "Record from failure AND success: if you only save corrections... may grow overly cautious" —— 只存纠正会越来越保守, 确认也要存 |
| project | 进行中的工作/目标/倡议/bug/事故 —— **不可从代码或 git 推导的** | 学到"谁在做什么、为什么、何时" | 相对日期必须转绝对日期("Thursday"→"2026-03-05"), 否则记忆过期后无法解读 |
| reference | 外部系统的指针: 去哪儿找什么 | 学到外部资源及其用途 | Linear 项目、Grafana 面板、Slack 频道这类"地址簿" |

feedback 和 project 的正文有固定结构:

```
规则/事实本身
**Why:** 用户给的理由(往往是过去的事故或强偏好)
**How to apply:** 何时何地生效
→ "Knowing *why* lets you judge edge cases
    instead of blindly following the rule"
```

不存什么 —— 五条排除(核心是不可推导原则: 凡是能从环境重新读到的, 不占记忆的座位):

```
- 代码模式/约定/架构/文件路径/项目结构 —— 读当前项目可推导
- git 历史/最近变更/谁改了什么 —— git log / git blame 是权威
- 调试方案/修复配方 —— fix 在代码里, commit message 有上下文
- CLAUDE.md 里已有的东西
- 临时任务细节: 进行中的工作/临时状态/当前对话上下文
```

显式保存门(注释标注 eval 验证过, 0/2 → 3/3):

- "These exclusions apply **even when the user explicitly asks you to save**. If they ask you to save a PR list or activity summary, ask what was *surprising* or *non-obvious* about it —— that is the part worth keeping."
- 用户明确要求存"本周 PR 列表"也一样排除 —— 反问"哪部分是意外/不显然的", 那才值得留

读侧防御(漂移警示):

- "Memory records can become stale... Before answering... verify that the memory is still correct... **If a recalled memory conflicts with current information, trust what you observe now** —— and update or remove the stale memory rather than acting on it."
- 记忆是"某时刻为真"的快照; 和现状冲突时信现状, 顺手更新或删除过期记忆

(prompt 指令视角的完整版 —— 指令树与四组英中对照, 见 [3.1 prompt](03.1-prompt.md) memory 节)

### 已覆盖部分(引用, 不重复)

- MEMORY.md 索引制 —— 存储视角速览见上节; prompt 指令视角见 [3.1 prompt](03.1-prompt.md) memory 节
- 运行时治理六步管线(boundary/budget/snip/microcompact/collapse/autocompact)与熔断 —— 见 [3.3.1 输入治理段](03.3.1-governance.md) query loop 第 1 段

---

## 总纲

- **上下文是工作内存。治理它的目标是支持系统继续工作**
- 三层制度各司其职: 稳定规则分层加载(不与临场对话混放)/ 长期记忆索引制(入口短, 正文密)/ 运行时预算治理(为失败和恢复留余地)
- **保留工作语义, 而非信息量**: 优先保文件、计划、错误修正、技能约束(直接决定下一步做对); 历史对话、重复附件、运行时随时可重取的东西不占座位
- 与 [3.3.1 输入治理段](03.3.1-governance.md) 的衔接: [3.3.1 输入治理段](03.3.1-governance.md) query loop 第 1 段 讲了每轮进模型前的治理**管线**, 本章讲的是治理的**制度** —— 管线围着 prompt cache 转, 制度围着预算转
