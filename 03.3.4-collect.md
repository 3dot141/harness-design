# 3.3.4 轮末收集段

## 4. 轮末收集段

### 源码细节

```
:1570  queuedCommandsSnapshot = getCommandsByMaxPriority(
          sleepRan ? 'later' : 'next')
        滤掉 slash 命令
        主线程: 只收 agentId === undefined 的
        子 agent: 只收发给自己的 task-notification
        (永不收 user prompt —— "哪怕有人往上盖了 agentId")

:1580  getAttachmentMessages(null, ctx, null, snapshot, 全历史, source)
        25 探测器跑一遍 → yield + 收进 toolResults
        ← attachment 通道的主注入点

:1602  memory 预取消费:
        settledAt !== null && consumedOnIteration === -1 才进
        没就绪 → 零等待跳过, 下轮迭代再试
        filterDuplicateMemoryAttachments(promise, readFileState)
        consumedOnIteration = turnCount - 1(防重复)

:1620  skill 预取收割: collectSkillDiscoveryPrefetch → attachment

:1630  command 生命周期:
        prompt / task-notification 型 → started
        + consumedCommandUuids 登记(给壳的 completed 用)

:1650  refreshTools: 新连的 MCP server 工具下轮可用

:1682  BG_SESSIONS: 周期任务摘要(claude ps 用)
        轮中触发, !agentId → REPL/SDK/HFI 都产, 子 agent 不产

:1679  nextTurnCount = turnCount + 1
:1700  maxTurns 检查(超 → yield max_turns_reached + return)
:1709  state = next {
          messages: [...治理后, ...assistant, ...toolResults],
          maxOutputTokensRecoveryCount: 0 }
        → continue 下一轮
```

### 总表

| # | 动作 | 解决的问题 | 怎么做 | 代价 |
|---|---|---|---|---|
| 1 | 队列快照 | 排队的用户输入 / task-notification 要择机注入 | 按 sleepRan 取 next/later 档; 主线程收无主消息, 子 agent 只收发给自己的 | 零 |
| 2 | getAttachmentMessages | 环境变化要告知模型 | 25 探测器全跑一遍 | 探测器自身成本 |
| 3 | memory 消费 | 检索结果注入 | 同步探测 settledAt, 就绪才 await | 零等待 |
| 4 | skill 收割 | 新技能提示注入 | collect → attachment | 零 |
| 5 | command 生命周期 | 排队命令状态可追踪 | started + 登记 uuid(壳的 completed 靠它) | 零 |
| 6 | refreshTools | 新连的 MCP server 要生效 | 刷新工具表, 下轮可用 | 零 |
| 7 | 收尾 | 下一轮准备 | turnCount+1 → maxTurns → state = next | — |

### memory 消费的三重防重

```
1. settledAt !== null     没结算 → 整个跳过, 下轮再试
2. consumedOnIteration    收过 → 不再收(= -1 才进)
3. filterDuplicateMemoryAttachments(promise, readFileState)
                          模型已亲自 Read/Write/Edit 过的
                          memory 不注入 — readFileState 是
                          跨迭代累计(per-iteration 数组
                          看不到更早轮次的操作)
```

### 队列过滤的越界防护

- 子 agent 永不收 user prompt, "哪怕有人往上盖了 agentId" —— 防消息被错误路由
- 否则通知被 removeFromQueue 却无处可附

### 总纲

- 轮末收集是下一轮增量输入的制造车间 —— attachment 通道的主注入点
- 与 1.(管历史形态)正交: 下一轮完整输入 = 1.(形态) × 4.(增量)
- 所有动作设计成零阻塞的探测式: 此刻任何等待都会推迟下一轮模型的启动; 等不起的东西(检索/发现)早已在 0. 预取段并行跑完

---
