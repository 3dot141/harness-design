# 3.9 checkpoint 体系

## 定位

- 代码回退 = **对话与文件的双轨时光机**: esc-esc(双击)或 `/rewind`(alias: `checkpoint`)打开消息选择器, 回到之前的任意一条用户消息
- 对话可以单独回退, 也可以连代码一起回退 —— 恢复到那条消息当时的文件状态
- 底层是三件事: **写前备份(trackEdit)→ 轮末快照(makeSnapshot)→ 按需恢复(rewind)**

---

## 1. 数据结构(fileHistory.ts)

```
FileHistoryState
├ snapshots[]            ≤ MAX_SNAPSHOTS(100), 旧的被逐
├ trackedFiles: Set      本会话动过的文件集合
└ snapshotSequence       单调计数 —— 即使快照被逐也递增
                          (给 useGitDiffStats 当活动信号:
                           snapshots.length 到顶后就不再变化)

FileHistorySnapshot
├ messageId              关联的用户消息(回退锚点)
├ trackedFileBackups     路径 → backup
└ timestamp

FileHistoryBackup
├ backupFileName         备份文件名; ★ null = 该版本文件不存在
├ version                v1, v2, ...(确定性命名 {hash}@v1)
└ backupTime
```

- `null` 语义让"回退到一个文件还不存在的时刻"(删除新建文件)成为一等操作

---

## 2. 三个操作

### trackEdit: 编辑前备份(写之前!)

- 调用时机: 工具改文件**之前** —— 注释原文: "This must be called before the file is actually added or edited, so we can save its contents before the edit"
- 三阶段提交:
  1. 检查: 已在最新快照里 → 直接返回
     ★ 防 speculative writes: 重复 trackEdit 会把确定性的 {hash}@v1 覆盖成 post-edit 内容, 污染 v1
  2. 异步备份(createBackup)
  3. 提交: 竞态复查(另一个 trackEdit 可能已插入)再写状态

### makeSnapshot: 每轮一个快照

```
对 trackedFiles 逐个(并行):
  文件没了(ENOENT)  → backupFileName: null("missing tracked file")
  mtime/内容没变     → 复用上一版备份(不重复占空间)
  变了              → 新版本 v+1
```

- 快照是**增量引用**而非全量拷贝: 未变的文件指向旧备份对象

### rewind: 恢复(纯文件系统副作用)

- 按 messageId 找目标快照(findLast)→ applySnapshot 把每个 tracked 文件恢复到该版本
- **不修改 FileHistoryState** —— 回退后再改文件, 会基于恢复后的内容产生新备份; 回退本身可逆
- 找不到快照 → 报错 "The selected snapshot was not found"

---

## 3. 调用全景: 模型零参与

- `src/tools/` 全部内置工具里**没有 RewindTool / CheckpointTool** —— checkpoint 不暴露给模型, 是纯 harness 内嵌机制
- 模型的角色只有一个: **触发写工具**; 备份、寻址、回滚全部在它视野之外

```
                模型可见?   触发者
──────────────────────────────────────────────────
写侧 trackEdit               harness(工具执行链内嵌)
  ├ FileEditTool.ts:435      x  模型只调 Edit, 备份是副作用
  ├ FileWriteTool.ts:259     x
  ├ NotebookEditTool.ts:312  x
  └ BashTool.tsx:393         x  (写文件落盘分支, 注释原文:
                                "Track file history before
                                making changes (for undo
                                support)" —— 写前备份)

快照 makeSnapshot            harness(轮边界)
  ├ QueryEngine.ts:645       x
  ├ REPL.tsx:3126            x
  └ handlePromptSubmit.ts:528 x  (用户提交 prompt 时)

读侧 rewind                  用户
  ├ /rewind 斜杠命令         x  supportsNonInteractive: false
  │   (commands/rewind/index.ts:6, alias: checkpoint)
  ├ REPL 消息选择器           x  (REPL.tsx:4966 onRestoreCode)
  └ print.ts:4550            x  (SDK/非交互恢复)
```

- 埋点条件(BashTool.tsx:392): `fileHistoryEnabled() && parentMessage` —— 备份挂在发起写入的那条 assistant 消息上(parentMessage.uuid), rewind 时按消息寻址

### 为什么 rewind 不做成模型工具

1. **trackEdit 是写路径的副作用**: 语义上属于执行链(权限检查后、落盘前), 内嵌零成本; 做成独立工具要求模型每次写文件前记得先调一次, 忘了就丢备份
2. **rewind 是跨消息的全局状态变更**: "回滚到哪个 turn"的裁判必须是用户 —— 模型没有可靠的自我定位, 且模型自持回滚权会打开"改错 → 自己回滚 → 再试"的静默循环
3. **会话控制权不下放**: /rewind 与 /compact、/clear 同类, 都是 session 级命令(command 层而非 tool 层, 分界见 [3.4 工具与权限](03.4-tools.md)), 这一层操作保留给用户 + harness

---

## 4. UI 层: MessageSelector(双击 esc 打开)

### 恢复选项(按 canRestoreCode 分支)

```
canRestoreCode(有文件变化)时:
  ├ Restore code and conversation   双轨
  ├ Restore conversation            只回对话
  └ Restore code                    只回代码
否则只有 conversation

附加选项:
  Summarize from here               从此点开始压缩
  Never mind                        取消
```

### 预览与确认

- diffStats(fileHistoryGetDiffStats): 选中的消息处**哪些文件会变 / 插入删除行数** —— 回退前先看代价
- canRestoreCode = fileHistory 启用 && filesChanged.length > 0
- 恢复分别执行、分别报错("Failed to restore the conversation and code: …")
- 确认页明示范围: "Confirm you want to restore the conversation / the code … to the point before"

### 恢复对话的实现

- restoreConversationDirectly: 截断 messages 到目标消息
- "Summarize from here": 对话截断 + 从该点生成摘要继续(等价于定点压缩)

---

## 5. 开关与默认

| 环境 | 条件 | 默认 |
|---|---|---|
| 交互会话 | fileCheckpointingEnabled !== false 且无 CLAUDE_CODE_DISABLE_FILE_CHECKPOINTING | **开** |
| SDK/非交互 | 需显式 CLAUDE_CODE_ENABLE_SDK_FILE_CHECKPOINTING | 关 |

- 默认开启 = 回退是产品能力, 不是实验; 关闭是显式退出

---

## 6. 设计要点

- **写前备份而非写后**: 备份的是"编辑前"的内容 —— 回退才有原始版本可回; speculative write 防污染是三阶段提交的关键
- **确定性命名 + 复用**: {hash}@v1 命名使重复备份天然去重; 快照间未变文件共享备份对象, 增量而非全量
- **null 表示不存在**: 删除文件/新建文件的回退被显式建模, 不是边界情况
- **回退不改历史状态**: rewind 只是文件副作用, FileHistoryState 原样 —— 回退可叠加、可再回退
- **代价前置可见**: diffStats 让用户在确认前看到影响面(文件数/行数)
- **模型零参与**: 写侧埋在写工具执行链、读侧只归用户命令 —— 模型触发写工具是唯一入口, 备份/寻址/回滚对它不可见
- 快照上限 100 + 被逐后 sequence 仍递增: 消费方(统计/UI)能区分"没有新活动"和"长度封顶"

---

## 总纲

- 代码回退 = **备份发生在写入之前、快照挂在消息上、恢复按消息寻址** —— 三个时机的配合
- 与 [3.3 query loop](03.3-query-loop.md) 的关系: 对话回退是"把 messages 截断到某条消息", 即 query loop 的输入直接变短, 下一轮从那里重新出发
- 与 compact([§3 compactConversation: 受控重启](03.5-context.md))的分工: compact 是"有损但连续地前进", rewind 是"无损但有界地后退"(100 快照窗口); Summarize from here 是两者的杂交 —— 回到过去再压缩着前进
