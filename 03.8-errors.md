# 3.8 错误与恢复

## 定位

- **错误属于主路径**: 设计只讲"正常情况下"会迅速露馅 —— 模型会被截断、请求会超长、hook 会制造回环、工具会中断、fallback 会发生、恢复逻辑本身也会失手。判断一个代理系统成不成熟, 不是看它顺畅时多像人, 而是看它出故障时像不像系统
- **错误处理保护的是执行叙事的一致性**: 系统还能不能说清"刚才试图做什么、为什么没做成、用了什么恢复路径、现在是继续/停止/换轨"。transition.reason、恢复计数、compact boundary、synthetic error message 都是**反失忆机制** —— 叙事一断, 工程对象退化成玄学对象
- 术语: PTL = prompt too long(输入超限, API 拒收)/ MOT = max output tokens(输出截断)/ media size error(图片 PDF 体积超限)—— 三者构成"可恢复错误"集合, 都走 withheld 先治后报

---

## 1. 恢复路径失败矩阵

| 事件 | 前置状态 | 下一步 | 出处 |
|---|---|---|---|
| PTL → collapse | staged collapse > 0 | recoverFromOverflow()(便宜先行) | [3.3.3 终局分派段](03.3.3-dispatch.md) |
| PTL → compact | staged = 0 | tryReactiveCompact(每回合一次) | 同上 |
| PTL → surface | hasAttempted 已 true | 直接浮出, 跳过 stop hooks | 同上 |
| compact 自身 PTL | compact 输入过长 | truncateHeadForPTLRetry(成组丢早期 API round, last-resort) | [§3 compactConversation: 受控重启](03.5-context.md) |
| MOT → 升档 | cap < MAX | 提 maxOutputTokensOverride | [3.3.3 终局分派段](03.3.3-dispatch.md) |
| MOT → 续写 | cap = MAX | meta user msg 续写(不 recap 不道歉), 限次 | 同上 |
| autocompact 连败 | consecutiveFailures >= 3 | 熔断: 跳过 compact, surface | [§2 预算阈值全景表](03.5-context.md) |
| 模型过载 | 529 连续 >= 3 | fallbackModel 重发(降级阶梯) | [3.3.2 流式消费段](03.3.2-streaming.md) |
| user abort | tool_use 悬空 | 消费剩余结果 + synthetic(账本必须闭合) | [3.3.3 终局分派段](03.3.3-dispatch.md) |

---

## 2. 熔断不变式(五条)

```
assert withheld_error ∈ {prompt_too_long, media_size, max_output_tokens}
  → 扣留只对可恢复错误(流式段 withheld 四来源)
assert hasAttemptedReactiveCompact ⇒ skip further reactive compact
  → 不自噬(State 重置策略逐站判定)
assert consecutiveFailures < MAX_CONSECUTIVE_AUTOCOMPACT_FAILURES(3)
  → 恢复系统自己也要受治理
assert compact aborted by user ⇒ ¬ summary_success
  → 中断不记成成功(APIUserAbortError, compact 的 abort 传给 forked agent)
assert every withheld recoverable_error surfaces iff recovery exhausted
  → 扣留必归还: 恢复耗尽才 surface
```

---

## 3. 回退模式同构收拢(七处)

| 场景 | 主路径 | 回退路径 | 出处 |
|---|---|---|---|
| 模型过载 | 主模型 | fallbackModel 重发 | [3.3.2 流式消费段](03.3.2-streaming.md) |
| 压缩 | session memory compaction | legacy 全量 compact | [§5 SessionMemory: 平时攒, 压缩时用](03.5-context.md) |
| 413 | collapse drain | reactive compact | [3.3.3 终局分派段](03.3.3-dispatch.md) |
| 输出截断 | 64k 升档 | meta 续写 | 同上 |
| 工具调度 | 流式 executor | 批式 runTools | [§2 调度: 从批式到流式](03.4-tools.md) |
| classifier 等待 | race 短超时 | 纯 await / mailbox | 同上 4 段 |
| 用户后悔 | 前进(compact) | 后退(checkpoint) | [3.9 checkpoint 体系](03.9-checkpoint.md) |

共同结构: 主路径失败 → 更保守/更贵的回退路径 → 都失败才 surface; 每级只试有限次, 保护位防循环。

---

---

## 总纲

- **代理系统的核心能力, 是出错后仍能继续工作** —— 恢复要可计数、可限次、可熔断, 否则恢复会从保险丝变成新的起火点
- checkpoint 体系(用户可操作的时光机)见 [3.9 checkpoint 体系](03.9-checkpoint.md)
