# 3.3.2 流式消费段

## 2. 流式消费段

### 源码细节

```
:650  while (attemptWithFallback)            ← fallback 重试的微循环
        callModel({                           ← deps 注入, 可 spy
          messages: prependUserContext(...),  ← userContext 此刻拼
          systemPrompt: fullSystemPrompt,
          signal: abortController.signal,     ← Escape 即断流
          options: { fastMode, fallbackModel, agents,
            maxOutputTokensOverride, mcpTools, queryTracking, ... } })

  for await (message of 流):
:826    assistant message → 收集 + 提取 tool_use blocks
          → toolUseBlocks + needsFollowUp = true
          → streamingToolExecutor.addTool(block, message)
             ← 边流边调度: 模型还在输出, 工具已开跑
        executor.getCompletedResults() 轮询:
          → yield result.message + 收进 toolResults
:820    API 错误 → withheld = true 扣留, 不 yield(先治后报)

:895  catch FallbackTriggeredError:
        currentModel = fallbackModel
        attemptWithFallback = true(整个请求重发)
        assistantMessages 清空
        yieldMissingToolResultBlocks(...)   ← 半截 tool_use 补账

:998  CACHED_MICROCOMPACT 延迟边界消息:
        流结束后按 API 实报删除量补 yield(累计-基线)
:1000 executePostSamplingHooks([...治理后消息, ...本轮输出])
        ← 分派前的最后一步, hooks 看到完整视图
```

### 总表

| # | 事件/动作 | 解决的问题 | 怎么做 | 代价 |
|---|---|---|---|---|
| 1 | 文本 yield | 用户要实时看到输出 | 边流边 yield | 零 |
| 2 | 边流边调度 | 等模型说完再跑工具, 浪费 2-30s | streamingToolExecutor.addTool 入队; getCompletedResults() 轮询收割 | 并发管理复杂度 |
| 3 | withheld 扣留 | 错误直接吐出去, 恢复就没机会了 | API 错误扣留不 yield; 恢复耗尽才 surface | 上层晚知道错误 |
| 4 | fallback 换模型 | 主模型过载/不可用 | 换 model 重发整个请求, 半截输出作废 | 必须 yieldMissingToolResultBlocks 补账 |
| 5 | 延迟边界消息 | 客户端估算 token 删除量不准 | 等 API 实报 cache_deleted_input_tokens(累计-基线) | 边界消息晚到一轮 |

### withheld 扣留: 先治后报

为什么要扣(query.ts:168 注释原文):

- 提前 yield 错误 → SDK 调用方(cowork/desktop)**见到 error 字段就终止会话**
- "the recovery loop keeps running but nobody is listening" —— 恢复还在跑, 但没人听了
- 扣留不是隐瞒, 是防止下游把可恢复错误当成终态

扣留的四来源(流内 :795-820, 任一命中即扣):

```
contextCollapse.isWithheldPromptTooLong   (PTL, collapse 想接管)
reactiveCompact.isWithheldPromptTooLong   (PTL, RC 想接管)
mediaRecoveryEnabled &&
  reactiveCompact.isWithheldMediaSizeError (media 尺寸, 需 gate)
isWithheldMaxOutputTokens                 (输出截断)
```

- "Either subsystem's withhold is sufficient — they're independent": 两个子系统独立, 关掉一个不破坏另一个的恢复路径

扣留 ≠ 丢弃:

- "Still pushed to assistantMessages so the recovery checks below find them"
- 消息照常进 assistantMessages; 3. 段的恢复链从 assistantMessages.at(-1) 找到它
- 扣的只是 yield(下游可见性), 不是数据

hoist 一致性(:623):

- mediaRecoveryEnabled 在流开始前算一次, 流内流后共用同一个值
- 理由: withholding(流内)与 recovery(流后)必须一致 —— CACHED_MAY_BE_STALE 的 feature 值可能在 5-30s 流期间翻转, withhold-without-recover 会把消息吃掉
- PTL 不用 hoist: 它的 withholding 是 ungated 的, 早于实验存在, 是 control-arm baseline

流前的孪生对照 —— 主动 preempt(:632):

- 进 API 前估算(tokenCountWithEstimation - snipTokensFreed)已到 blocking limit → 直接造 PTL 错误 yield 出去 + return blocking_limit
- 同一类错误, 此刻**不扣** —— 没有恢复机会, 立即报
- 精细的跳过条件(把恢复机会留给真 413):
  - compact / session_memory query: forked agent 继承全对话, 会死锁
  - RC 启用 + 自动压缩允许: synthetic error 在 API 调用前返回, RC 永远看不到真 PTL
  - collapse 启用: synthetic preempt 会饿死 recoverFromOverflow 的 drain
  - isAutoCompactEnabled() 联词: 用户显式 "no automatic anything" 配置仍得到 preempt

时序:

```
流内: 错误消息到达
  ├ 判定四来源 → withheld=true → 不 yield(下游看不见)
  ├ 照常 push 进 assistantMessages
  └ 流结束 → 3. 恢复链尝试
       ├ 恢复成功 → continue(错误从未到达下游)
       └ 恢复耗尽 → yield lastMessage + executeStopFailureHooks
                    (此刻才 surface, 且不进 stop hooks)
```

代价与对冲:

- 代价: 上层晚知道错误(恢复期间几秒到几十秒)
- 对冲: 恢复有限次(single-shot / escalate / 限次 3)→ 最坏有界延迟; executeStopFailureHooks 保证 stop-failure hooks 看得到最终错误
- 反面: 不扣的话错误一吐, SDK 即终止会话, 恢复跑完也无人接收

### 案例: 一次 413 的完整扣留-恢复

场景: 上下文 190k, API 上限 200k, 发请求 → 返回 413:

```
t0  API 返回 413 —— 错误消息作为流里一条 message 到达
t1  判定: reactiveCompact.isWithheldPromptTooLong = true → 不 yield
    (UI 安静, SDK 安静, 会话还活着)
t2  错误消息照常 push 进 assistantMessages(本地数组, 没往外吐)
t3  流结束, 进入 3. 终局分派
t4  分派段检查 assistantMessages.at(-1): 是被扣的 413 → 走恢复链
t5  恢复链修病根:
    5a collapse drain: 提交暂存折叠, 190k → 150k
    5b 还不行 → reactive compact: 全量压缩 → 60k
t6  恢复成功 → state = next → continue(回循环开头)
t7  重新治理 → 重新调 API(60k, 这次成功)
t8  模型继续输出, 工具继续跑
    ★ 对 UI/SDK: 413 从未存在过, 只是这轮慢了几秒
```

反面对照(如果不扣):

```
t1' yield 413 → SDK 见 error 字段
t2' SDK 按契约终止会话(下游约定: error = 终态)
t3' 恢复循环还在跑: 压缩成功, 重新调用也成功
t4' 但会话已死, 没人在听 —— 白干
```

- 扣留 = 给运行时争取一次"先修后报"的机会窗口: 修好了, 错误降级成"这轮慢几秒"; 修不好, 才连同"我修过、没修成"的事实一起交出(executeStopFailureHooks)
- 晚知道的那几秒正是恢复链干活的时间, 且有界(恢复限次)

### 边流边调度: StreamingToolExecutor(概要)

把"模型输出"和"工具执行"两个阶段重叠起来:

```
串行: 等 → 执行 → 交回
模型流: [文本..][tool A][tool B][tool C] ──全部流完──→ 解析 → A → B → C → 结果发回
        └────────── 必须等完 ──────────┘         └── 串行执行 ──┘

重叠: tool_use block 在流中"成形"的瞬间就开跑
模型流: [文本..][tool A 成形]·······[tool B 成形]·····[tool C 成形]···(还在输出)
                    ↓ addTool                ↓                ↓
执行:              A 开跑 ───────────── B 开跑(并发安全时)   C 排队
                    └── A 跑完 → getCompletedResults() 收割
```

- 源码依据(query.ts:843): for await 每收到一段成形的 assistant 内容, 提取出 tool_use block 就 streamingToolExecutor.addTool(block, message) —— 不等整条消息流完
- 省下的时间 = 前几个工具的执行时长, 藏进模型还在输出的那几秒里

- 完整机制(状态机 queued→yielded / 并发规则 canExecuteTool / 乱序执行有序输出 / 三级取消 / 为什么敢抢跑的三个对冲)与批式 runTools 的演进关系(gate 二选一、A/B 实验、契约先立芯不动), 见 **[3.4 工具和权限](03.4-tools.md) 工具和权限 2. 调度: 从批式到流式**

一句话: 串行架构里"输出"和"执行"是接力, 这里是重叠 —— 这恰好是"驱动-调度-反馈"取代"请求-响应"的具体形态。

### fallback: 模型回退的降级阶梯(withRetry.ts)

模型回退的主干就发生在本段 —— while (attemptWithFallback) 微循环包着流式段, 换模型重发是它的 catch(:895)。同一个请求失败时从轻到重逐级降级:

```
请求失败(429/529/参数错)
│
├─ 第 1 级: fast mode 冷却
│    触发容量问题 → triggerFastModeCooldown(cooldownMs)
│    → 本请求 retryContext.fastMode = false(降标准速度重试)
│
├─ 第 2 级: fast mode 永久禁用
│    API 拒绝 fast mode 参数(org 未开通)
│    → handleFastModeRejectedByAPI(), 标准速度重试
│
├─ 第 3 级: 529 计数重试
│    连续 529 < MAX_529_RETRIES(3)→ 常规退避重试
│
└─ 第 4 级: 模型 fallback
     连续 529 >= 3 且配置了 fallbackModel
     → throw FallbackTriggeredError(model → fallbackModel)
```

触发条件(withRetry.ts:323):

```
consecutive529Errors >= 3
AND (FALLBACK_FOR_ALL_PRIMARY_MODELS env
     OR (非 Claude AI 订阅者 AND 主模型是非自定义 Opus))
AND options.fallbackModel 已配置
```

- 降级状态记在**请求上下文**里(retryContext.fastMode), 不污染会话 State
- 阶梯顺序的含义: 先试"同模型降速", 再试"换模型" —— 换模型破坏缓存, 成本最高, 放最后

前台/后台的不对称待遇(529):

```
FOREGROUND_529_RETRY_SOURCES(白名单: repl 等前台源)
  → 529 重试 + 可 fallback
其余源(摘要/标题/建议/classifier 等后台调用)
  → 529 直接放弃 —— "no retry amplification during capacity cascades"
```

- 容量级联时后台小任务重试只会火上浇油 —— 次要工作先弃船, 前台工作才挣扎; 后台失败是静默的

传播链(claude.ts:2599 注释: "FallbackTriggeredError must propagate to query.ts, which performs the retry" —— API 层不处理换模型, query loop 才是重试的家):

```
query.ts:895 catch:
  currentModel = fallbackModel; attemptWithFallback = true(重发整个请求)
  assistantMessages 清空 + 双层补账(见下节 ①)
效果: 对下游, fallback 表现为"这一轮慢了一点", 半截输出从未泄漏
```

### 补账原则的三处落地

```
补账原则: 只要系统向外承诺了一段执行,
中断/作废时必须有配套交代 ——— 三处落地:

1. fallback 重发(query.ts:895)   半截 assistant 里的 tool_use
                                → yieldMissingToolResultBlocks
2. abort 有 executor(:1012)      executor 检查 abort 信号
                                → 自动生成 synthetic tool_result
3. abort 无 executor(:1020)      手动 yield 透漏块
                                + 'Interrupted by user' 说明
```

什么时候触发 —— 共同前提: API 协议要求每个 tool_use 必有配对 tool_result, 下一轮请求不配对直接 400。所以任何"tool_use 已流出、但结果不会到来"的时刻都必须补:

| 落地点 | 触发事件 | 时刻 | 补给谁 |
|---|---|---|---|
| 1. fallback 重发 | 换模型重发整个请求(FallbackTriggeredError) | 流式中途 catch | 半截 assistant 里已成形的 tool_use —— 重发后旧的作废 |
| 2. abort + executor | 用户按 ESC | 流结束检查点(abort 判定最优先) | queued/executing 中的工具, getRemainingResults() 排干 |
| 3. abort 无 executor | 用户按 ESC(executor 未启用) | 同上 | 已流出的 tool_use, 手动 yield 透漏块 |

- 典型链: 模型输出到一半用户按 ESC → 已成形的 tool_use 不会执行 → 不补则下轮 400, 会话死掉
- executor 内部还有第四处(sibling_error / streaming_fallback 的 synthetic error), 属三级取消机制, 不在 query.ts 层

- callModel 是 deps 注入的(6-8 个测试文件 spy 它)—— 流式段所有行为可测
- State 通过 options 流进 API 调用: maxOutputTokensOverride(3. 的升档值)、agents / allowedAgentTypes、mcpTools 在此处生效 —— 分派段写的 State, 下一轮从这里兑现
- 流结束后、分派前: executePostSamplingHooks 拿 [...治理后消息, ...本轮输出] —— PostSampling hooks 看到的是完整视图

### 总纲

- 流式段把模型从"一次调用"变成一条事件流, 运行时由此获得三个原本没有的能力:
  - 实时可见(yield)
  - 并行调度(边流边跑工具)
  - 抢先治理(withheld 扣住错误先试恢复)
- 架构从"请求-响应"变成"驱动-调度-反馈" —— 这就是"模型调用只是心跳的一次收缩"的工程含义

---
