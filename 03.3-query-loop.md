# 3.3 query loop

## 定位

- `query()`(query.ts:219)只是壳: 负责 command 生命周期补账 —— 正常返回才发 completed, throw 时留下 started-无-completed 的非对称信号
- `queryLoop()`(query.ts:241)是芯: 一个携带跨迭代 State 的 while(true) 循环
- 一个系统能否被称为 agent, 不在会不会说, 在几轮之后还知不知道自己在做什么 —— lifecycle 是关键名词

```
query()  ─── 壳: command 生命周期补账(正常返回才发 completed;
└─ queryLoop()    throw 时留 started-无-completed 非对称信号)
   │
   │  State(10 字段, 每轮整体替换 state = next)
   │  messages / toolUseContext / autoCompactTracking /
   │  maxOutputTokensRecoveryCount / hasAttemptedReactiveCompact /
   │  maxOutputTokensOverride / pendingToolUseSummary /
   │  stopHookActive / turnCount / transition(记"上轮为何 continue")
   ▼
┌─────────────────────────────────────────────────┐
│ 0. 预取(穿插点火, 与后续所有段并行)                │
│    memory: 循环外点火, 每 user turn 1 次          │
│    skill:  每迭代点火, findWritePivot 早退         │
├─────────────────────────────────────────────────┤
│ 1. 输入治理段(每迭代, 进模型前)                    │
│    管的是: 模型看到什么【形态的历史】               │
├─────────────────────────────────────────────────┤
│ 2. 流式消费段(for await 事件流)                    │
│    模型变事件流: 可见 / 可并行 / 可抢先治理         │
├─────────────────────────────────────────────────┤
│ 3. 终局分派段(先判定这轮怎么收场)                   │
│    重试也是被管理的行为                            │
├─────────────────────────────────────────────────┤
│ 4. 轮末收集段(仅 follow-up 主路径)                 │
│    管的是: 下一轮【追加什么新信息】                 │
└─────────────────────────────────────────────────┘
   ▼
 turnCount+1 → maxTurns 检查 → state = next → continue
```

四段各一句话:

| 段 | 一句话本质 | 子页 |
|---|---|---|
| 0. 预取 | 把检索延迟藏进空闲(频次控制压浪费) | [3.3.0 预取段](03.3.0-prefetch.md) |
| 1. 治理 | 历史形态管理, 破坏性递增, 围着 prompt cache 转 | [3.3.1 输入治理段](03.3.1-governance.md) |
| 2. 流式 | 模型变事件流: 可见、可并行、可抢先治理 | [3.3.2 流式消费段](03.3.2-streaming.md) |
| 3. 分派 | 重试是被管理的行为, 保护位逐站判定 | [3.3.3 终局分派段](03.3.3-dispatch.md) |
| 4. 收集 | 增量输入制造车间, 全探测式零阻塞 | [3.3.4 轮末收集段](03.3.4-collect.md) |

### 术语: REPL

REPL = Read-Eval-Print Loop(读-求值-打印-循环), Lisp 世界传下来的术语。主交互会话就是这个结构:

```
Read   ← 读用户输入(processUserInput)
Eval   ← "求值" = query loop 跑一轮(治理→调模型→工具→分派)
Print  ← 流式输出到终端
Loop   ← 等下一个输入, 循环
```

代码里 REPL 泛指**有人坐在终端前的交互式主会话**:

| 出现处 | 含义 |
|---|---|
| src/screens/REPL.tsx | 主界面组件(整个终端 UI) |
| querySource: 'repl_main_thread' | 主线程标记 —— 区别于子 agent / forked query(各有自己的 querySource, 如 'compact'、'agent:xxx') |
| REPL 数组 / mutableMessages | 主会话的**完整消息历史**(UI 显示、transcript 用) |

文档里反复出现的"REPL 原件 vs 投影/视图"分界, 本质是**两个数组服务两个主人**:

```
mutableMessages(REPL 的)      messagesForQuery(query 的)
完整历史                       界碑后 + 治理后的瘦身版
给 UI 显示 / 审计 / 回退        给模型看
```

collapse 的读时投影、snip 的"剪了但 transcript 还在"、boundary 截取, 都是在这两个数组之间做文章 —— **显示层永远完整, 模型层永远瘦身**。与之相对的形态是 headless/SDK 模式(没人坐终端, 程序化调用), 很多 feature gate 会区分这两种(如 fileCheckpointing 默认只在交互开)。

---

## 贯穿性设计主题

### 1. 输入 = 形态 × 增量

- 1. 治理管形态: 已有历史怎么裁剪/投影/压缩(缓存友好, 破坏性递增)
- 4. 收集管增量: attachment 通道追加什么新信息(文件变更 / todo / memory / 新技能 / 队列命令)
- 两个方向正交, 互不干涉

### 2. 上下文形态稳定性(与 prompt 部分一脉相承)

- prompt 静态区: 防前缀碎片(2^N 问题, 界碑设计)
- 治理段: 防历史变形(冻结已见结果 / 缓存编辑 / 读时投影)
- 同一个设计目标在两个部件的落地

### 3. 重试是被管理的行为

- State 字段的重置策略逐站定制, 不是模板
- 死循环防护靠"知道什么时候不该再试", 不是靠重试上限

### 4. 成本决定通道: skills 的"报户口"与"找新人"

skill 在 query loop 里出现两处, 不是重复 —— 是两个不同能力按成本拆通道:

| | skill_listing / dynamic_skill(attachment 探测器) | startSkillDiscoveryPrefetch(query.ts:331) |
|---|---|---|
| 做什么 | 列已加载技能的清单/变化(delta) | 发现新技能(语义搜索) |
| 怎么做 | 本地枚举 + 对比上轮, 有变化才注入 | Haiku 提词 + AKI 语义查询, 跑 sideQuery |
| 成本 | 毫秒级, 纯本地 | 秒级(AKI@250ms ~ 3.3s) |
| 频次 | 每轮轮末跟着 25 探测器跑 | 每迭代点火 + write-pivot guard |

- listing 是"报户口": 技能已加载, attachment 只是把"现在有哪些可用/哪些变了"告诉模型(渐进披露: 索引一行常驻, 不展开)
- discovery 是"找新人": 从信号(用户输入/写操作)判断有没有尚未注册的技能值得浮出 —— 要跑模型做语义判断, 贵

演进史(attachments.ts:876 注释原记): discovery 曾在 getAttachmentMessages 里, 是阻塞调用(assistant_turn 信号, 每轮同步跑 Haiku)—— prod 里 97% 什么都没发现, 纯付延迟。所以才拆出去: query loop 点火预取、轮末收割, 把秒级延迟藏进主 turn。

```
便宜(本地枚举, 毫秒)
  → 留在 attachment 探测器里, 轮末同步跑

贵(跑模型/语义检索, 秒级)
  → 拆出 attachment 管道, 循环内点火预取:
     memory 预取(:297)         每 user turn 1 次
     skill discovery(:331)     每迭代 + write-pivot guard
     turn-0 那次               仍留在 userInputAttachments 阻塞
                               (第 0 轮没有可掩护的工作, 只能付)
```

一句话: 成本决定通道 —— 报户口的走探测器, 找新人的必须预取。与 memory 预取是同一条原则。

---
