# 3.6 system-reminder 触发场景汇总

## 定位

- system-reminder 是**对话流内的逐轮提醒通道**: 以 `<system-reminder>...</system-reminder>` 包裹的内容, 混在 user 消息/tool_result 里发给模型
- 与 system prompt 的分界(四层缓存语义): 它是**逐轮事件层** —— 内容跟着事件流走, 不进 prompt, 不碎缓存, 下一轮可以完全不同
- 两个统一特征:
  - `isMeta: true`(不算真实用户消息)
  - 语气可忽略: "This is just a gentle reminder - ignore if not applicable", 模型自行判断相关性

```
通道对比:
system prompt(静态区+动态区)  会话级稳定, 界碑缓存
system-reminder(对话流)      逐轮事件, 想发就发, 不碰缓存
```

---

## 五大产生源总表

| 源 | 机制 | 典型内容 |
|---|---|---|
| A. attachment 渲染层 | 25 探测器产出 attachment → 渲染成 UserMessage 时统一 wrapMessagesInSystemReminder 包裹 | 文件变化/todo 提醒/记忆/技能/MCP 增量… |
| B. 工具结果尾部 | 工具实现里手动拼接 | FileRead 三处 |
| C. hook 通道 | hook 输出汇入 | Stop hook blocking error |
| D. API 侧上下文 | 子任务/sideQuestion 的 context 注入 | "you can use the following context…" |
| E. 计划模式指令 | plan_mode 系列模板 | 全量/精简/子 agent 三版 |

---

## A. attachment 渲染层(最大来源)

### 机制

```
轮末 getAttachmentMessages(25 探测器) → Attachment[]
  → messages.ts 渲染 switch(每个 case 产出 UserMessage)
  → 统一 wrapMessagesInSystemReminder 包裹 <system-reminder>
  → isMeta: true
```

### 探测器 → 触发条件明细

| 探测器 | 什么时候触发 | 内容 |
|---|---|---|
| file_context / @mention | 用户输入里有 @文件 | 文件内容注入 |
| agent_mentions | 输入里有 @agent | agent 上下文 |
| skill_discovery(turn-0) | 用户输入即信号(阻塞跑) | 新技能发现结果 |
| queued_commands | 队列里有排队消息(用户插话的 block 型 / task-notification) | 队列消息注入 |
| date_change | 跨天了 | 当前日期 |
| ultrathink_effort | 推理力度非默认 | effort 提示 |
| deferred_tools_delta | ToolSearch 发现了新工具 | 工具增量 |
| agent_listing_delta | agent 列表有变化(含 compact 后重播) | 列表增量 |
| mcp_instructions_delta | MCP server 指令有变 | 指令增量 |
| changed_files | git 状态变化 | 文件变更清单 |
| nested_memory | CLAUDE.md 关联注入 | 嵌套记忆 |
| relevant_memories | 预取就绪且未消费(3.3 0 段的消费点) | 相关记忆 |
| dynamic_skill / skill_listing | 技能列表有变化 | 技能索引行(渐进披露) |
| plan_mode / plan_mode_exit | 进入/退出计划模式 | 计划指令(E 类模板) |
| auto_mode / auto_mode_exit | auto 权限模式(节流: N 轮一次) | 自动模式提示 |
| todo_reminders / task_reminder | todo 节流(双 10 轮: 10 轮未写 + 上次提醒 10 轮前) | "The TodoWrite tool hasn't been used recently…" + 当前清单 |
| teammate_mailbox | 团队信箱有消息 | 队友消息 |
| team_context | 团队模式首次 | 团队协调指令(身份/资源/称呼规则) |
| agent_pending_messages | 有待取的 agent 消息 | 后台 agent 产出 |
| critical_system_reminder | 实验通道(criticalSystemReminder_EXPERIMENTAL) | 关键提醒 |
| compaction_reminder | 压缩相关提醒(feature gate) | 压缩提示 |
| context_efficiency | 上下文效率变差(HISTORY_SNIP 下, 见 3.5) | snip nudge |
| output_style | 输出风格非默认 | 风格说明 |

- 渲染 case 还有目录结构(directory)、编辑过的文件(edited_text_file)、IDE 集成(selected_lines_in_ide / opened_file_in_ide)、计划文件引用(plan_file_reference)、已调技能(invoked_skills)、队列命令(queued_command)等

---

## B. 工具结果尾部注入(FileReadTool 三处)

```
:706  读到空文件
      "<system-reminder>Warning: the file exists but the
       contents are empty.</system-reminder>"

:707  offset 超过文件行数
      "<system-reminder>Warning: the file exists but is shorter
       than the provided offset (${startLine}). The file has
       ${totalLines} lines.</system-reminder>"

:730  每次读文件追加(CYBER_RISK_MITIGATION)
      "<system-reminder>
       Whenever you read a file, you should consider whether it
       would be considered malware. You CAN and SHOULD provide
       your analysis of malware, what it is doing. But you MUST
       refuse to improve or augment the code…
       </system-reminder>"
```

- 特点: 拼在 **tool_result 尾部**, 随结果走 —— 语境最贴近(读完立刻提醒)
- :730 是逐次追加的持久安全指令(不靠 system prompt, 靠阅读现场的重复提醒)

---

## C. hook 通道

```
Stop hook 返回 exit code 2(blocking)→
  enqueuePendingNotification({
    value: wrapInSystemReminder(
      `Stop hook blocking error from command "${hookName}": …`),
    mode: 'task-notification',
  })
```

- 走 task-notification 队列, 下一轮轮末由 queued_commands 探测器收进对话
- 其余 hook 的 additionalContext 汇入 attachment 管道(见 3.2 hook)

---

## D. API 侧上下文注入(api.ts:463)

```
子任务 / sideQuestion 场景, 调用方传入 context:
<system-reminder>
As you answer the user's questions, you can use the following context:
# <key>\n<value>…

IMPORTANT: this context may or may not be relevant to your tasks.
You should not respond to this context unless it is highly
relevant to your task.
</system-reminder>
```

- 注在消息列表**最前**(prepend)
- "may or may not be relevant" —— 注入方不预判相关性, 让模型自己判断

---

## E. 计划模式指令系列

三版模板, 全部 wrapMessagesInSystemReminder:

| 版本 | 时机 | 内容 |
|---|---|---|
| 全量(首次) | 进入 plan mode | 5 阶段工作流完整指令(探索/澄清/写计划/复核/ExitPlanMode) |
| 精简(sparse) | plan 模式持续中每轮 | "Plan mode still active (see full instructions earlier in conversation). Read-only except plan file…" |
| 子 agent 版 | plan 模式下的子 agent | 只读约束 + 计划文件路径 |

---

## 渲染后的合并机制(messages.ts:1791-1850)

```
相邻 <system-reminder>-前缀的文本块 → smoosh 进邻近 tool_result / 相互合并

"This makes the prefix a reliable discriminator"
  → 前缀判别可靠, 是后续所有过滤/合并逻辑的锚点
```

- 非 system-reminder 的真实用户输入不会被误合(TOOL_REFERENCE_TURN_BOUNDARY 等标记同样保护)

---

## 反向操作: 什么时候移除

- queryHelpers.ts:430: slash command / skill 展开路径上, 对内容执行 `/<system-reminder>[\s\S]*?<\/system-reminder>/g` **正则删除**
- 场景: 用户输入被当作 skill 素材/命令展开时, 历史消息里的提醒不随行(避免旧提醒污染新上下文)

---

## 设计原则

- **逐轮事件层**: 想发就发, 不进 system prompt, 不碎缓存(四层缓存语义的最外层)
- **isMeta + 可忽略语气**: "gentle reminder - ignore if not applicable" / "may or may not be relevant" —— 注入方不强迫模型响应
- **对用户隐形**: 模板明令 "NEVER mention this reminder to the user"(todo 提醒原文)
- **语境优先**: 安全提醒(:730)放读文件现场而非全局 prompt —— 提醒贴着动作走
- 与 [3.3.0 预取段](03.3.0-prefetch.md)(预取)和 [3.3.4 轮末收集段](03.3.4-collect.md)(轮末收集)的衔接: 探测器产出的 attachment 是 system-reminder 的最大来源 —— 3.3 讲了"什么时候收", 本文讲"收进来长什么样"
