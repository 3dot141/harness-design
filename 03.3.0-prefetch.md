# 3.3.0 预取段

## 0. 预取段

### 源码细节

```
query.ts:297(循环外, 每 user turn 1 次)
  using pendingMemoryPrefetch = startRelevantMemoryPrefetch(...)
      │
      ├ 静默不 fire 的 5 个条件(attachments.ts:2361):
      │   auto memory 未开 / feature gate 关
      │   找不到非 meta 的 user 消息
      │   单词 prompt(无空格 = 无上下文可提词)
      │   本会话已注入 memory 字节超 MAX_SESSION_BYTES
      │   预取出错 → catch 吞掉返回 [](永不碰主流程)
      │
      ├ 检索输入: 最后一条真实 user 文本
      │   + activeAgents + readFileState(累计读过的文件)
      │   + 最近成功工具 + 已 surface 的 memory 路径
      │
      └ handle 三件套:
          promise(异步跑) / settledAt(finally 才设)
          consumedOnIteration(-1 起步)
          [Symbol.dispose]: abort + 终端遥测
              ↳ 不用给循环内 ~13 个 return 逐个加清理

query.ts:331(循环内, 每迭代)
  pendingSkillPrefetch = startSkillDiscoveryPrefetch(...)
      ├ findWritePivot guard: 上轮无写操作 → 早退
      └ turn-0 的孪生路径在别处阻塞跑:
         userInputAttachments 里的 getTurnZeroSkillDiscovery
         (第 0 轮没有可掩护的工作 → 只能阻塞, 一次性付)
```

### 总表

| # | 动作 | 解决的问题 | 怎么做 | 代价 |
|---|---|---|---|---|
| 1 | memory 预取 | 相关记忆检索要跑 sideQuery(~1s), 轮末同步等太贵 | user turn 开局 fire-and-forget; 轮末只做同步探测 settledAt | 一条并行 sideQuery; 没赶上就放弃本 turn |
| 2 | skill 预取 | 技能发现查询更贵, 且 prod 97% 无发现 | 每迭代 fire, findWritePivot guard(上轮无写操作 → 早退) | 同上; guard 让无写迭代零成本 |

### 机制展开

- 静默不 fire 的五个条件(attachments.ts:2361):
  - auto memory 未开 / feature gate 关
  - 找不到非 meta 的 user 消息
  - 单词 prompt(无空格 = 无上下文可提词)
  - 本会话已注入 memory 字节超 MAX_SESSION_BYTES
  - 预取出错 → catch 吞掉返回 [](永不碰主流程)
- handle 三件套:
  - `settledAt`: promise.finally 才设, 同步探测的依据
  - `consumedOnIteration`: -1 起步, 消费后打标防重复
  - `[Symbol.dispose]`: abort in-flight + 终端遥测
- `using` 声明(query.ts:297): 循环里 ~13 个 return 站点不用逐个加清理, generator 任何退出路径(正常/throw/.return())自动 dispose
- 子 abort controller 链到 turn 级: 用户按 Escape 立即取消 sideQuery, 不等 queryLoop 退出
- turn-0 的孪生路径: 用户输入即信号的发现查询阻塞执行(userInputAttachments 里 getTurnZeroSkillDiscovery)——"第 0 轮没有可掩护的工作", 藏不了就一次性付
- 反例存档(attachments.ts:795): `skipSkillDiscovery` gate —— 技能被调用时 SKILL.md 全文作为 input 提 @-mention, 不是用户意图, 不得触发发现; 没这个 gate, 110KB SKILL.md 每次调用烧 ~[3.3 query loop](03.3-query-loop.md)s 分块查询

### 总纲

- 预取的本质: 把检索延迟藏进模型流式+工具执行的空闲里
- 两条预取各自用频次控制压浪费:
  - memory 靠 "prompt 不变所以只问一次"
  - skill 靠 "无写操作所以不问"
- 量化(注释): >98% 在 turn 结束前 resolve(AKI@250ms / Haiku@573ms vs turn 2-30s)—— 命中即免费, 未命中零损失

---
